# 🚀 دليل التشغيل الكامل - Cyber Mirage v5.0

## 📍 **أنت الآن هنا:**
```
✅ Python 3.13.5 موجود
✅ Virtual Environment موجود
✅ المشروع 100% كامل
```

---

# 🎯 **3 طرق للتشغيل**

## ══════════════════════════════════════════════════════════
## **الطريقة 1: تشغيل بسيط (الأسهل)** ⭐ مُوصى به للبداية
## ══════════════════════════════════════════════════════════

### **ما تحتاجه:**
- ✅ Python (موجود بالفعل)
- ✅ Virtual Environment (موجود بالفعل)
- ❌ لا تحتاج Docker أو برامج إضافية

### **الخطوات:**

#### **1️⃣ افتح PowerShell في المشروع**
```powershell
cd A:\cyber_mirage
```

#### **2️⃣ شغّل الـ Defense System**
```powershell
.\start_defense.ps1
```

**ماذا سيحدث؟**
```
✅ سيفتح 3 نوافذ PowerShell:
   1. Dashboard (Streamlit) - واجهة المراقبة
   2. Honeypot Environment - البيئة الأساسية
   3. Log Monitoring - مراقبة اللوجز

✅ سيظهر:
   • Dashboard على: http://localhost:8501
   • IP الخاص بك: 192.168.1.3
```

#### **3️⃣ افتح Dashboard**
```
افتح المتصفح واذهب إلى:
http://localhost:8501
```

#### **4️⃣ للإيقاف**
```powershell
# أغلق النوافذ الـ 3 اللي فتحت
# أو اضغط Ctrl+C في كل نافذة
```

---

## ══════════════════════════════════════════════════════════
## **الطريقة 2: Docker Production (الأفضل)** 🐳 للاستخدام الفعلي
## ══════════════════════════════════════════════════════════

### **ما تحتاجه:**
- ✅ Python (موجود)
- ❌ Docker Desktop (محتاج تحمله)

### **الخطوات:**

#### **A. تحميل Docker Desktop** (مرة واحدة فقط)

##### **Windows:**
```
1. اذهب إلى: https://www.docker.com/products/docker-desktop
2. اضغط "Download for Windows"
3. شغّل الملف المُحمل
4. اتبع خطوات التثبيت
5. أعد تشغيل الكمبيوتر
6. افتح Docker Desktop وانتظر حتى يبدأ
```

##### **أو باستخدام Chocolatey:**
```powershell
# إذا عندك Chocolatey
choco install docker-desktop
```

#### **B. تأكد من تشغيل Docker**
```powershell
# افتح Docker Desktop أولاً
# ثم اختبر:
docker --version
docker-compose --version
```

#### **C. تحضير Environment Variables**
```powershell
# 1. انسخ ملف الـ .env
Copy-Item .env.example .env

# 2. افتح الملف للتعديل
notepad .env

# 3. غيّر الباسوردات:
#    - POSTGRES_PASSWORD=YourSecurePassword123!
#    - REDIS_PASSWORD=YourRedisPassword456!
#    - GRAFANA_PASSWORD=admin123
```

#### **D. تشغيل Production Stack**
```powershell
# شغّل كل شيء
.\deploy_production.ps1 -Action start

# انتظر 2-3 دقائق حتى يبدأ كل شيء
```

#### **E. تحقق من الحالة**
```powershell
.\deploy_production.ps1 -Action status
```

#### **F. افتح الخدمات**
```
✅ Dashboard:    http://localhost:8501
✅ Grafana:      http://localhost:3000 (admin/admin123)
✅ Prometheus:   http://localhost:9090
✅ Alertmanager: http://localhost:9093
```

#### **G. للإيقاف**
```powershell
.\deploy_production.ps1 -Action stop
```

#### **H. لإعادة التشغيل**
```powershell
.\deploy_production.ps1 -Action restart
```

#### **I. لمشاهدة اللوجز**
```powershell
# كل اللوجز
.\deploy_production.ps1 -Action logs

# لوج خدمة معينة
.\deploy_production.ps1 -Action logs -Service ai-engine
.\deploy_production.ps1 -Action logs -Service dashboard
.\deploy_production.ps1 -Action logs -Service honeypots
```

---

## ══════════════════════════════════════════════════════════
## **الطريقة 3: Attack Testing (للاختبار)** 🔴 متقدم
## ══════════════════════════════════════════════════════════

### **ما تحتاجه:**
- ✅ System قيد التشغيل (الطريقة 1 أو 2)
- ❌ Kali Linux VM (للهجمات)
- ❌ VirtualBox أو VMware

### **الخطوات:**

#### **A. تحميل Kali Linux**
```
1. اذهب إلى: https://www.kali.org/get-kali/
2. حمّل: Kali Linux VirtualBox 64-Bit (OVA)
3. حجم الملف: ~3-4 GB
```

#### **B. تثبيت VirtualBox**
```
1. اذهب إلى: https://www.virtualbox.org/
2. حمّل VirtualBox لـ Windows
3. ثبّت البرنامج
```

#### **C. استيراد Kali Linux**
```
1. افتح VirtualBox
2. File → Import Appliance
3. اختر ملف Kali .ova
4. اضغط Import
5. انتظر (5-10 دقائق)
```

#### **D. تكوين الشبكة**
```
1. اختر VM في VirtualBox
2. Settings → Network
3. Adapter 1: Bridged Adapter
4. اختر network card الخاص بك
5. اضغط OK
```

#### **E. تشغيل Kali**
```
1. شغّل الـ VM
2. Username: kali
3. Password: kali
4. سجّل دخول
```

#### **F. اختبار الاتصال**
```bash
# في Kali Linux Terminal
ping 192.168.1.3
```

#### **G. تشغيل الهجمات**
```bash
# نسخ سكريبت الهجمات من Windows
# أو اكتب في Kali:

# 1. Port Scan
nmap -sV 192.168.1.3

# 2. SSH Brute Force
hydra -l admin -P /usr/share/wordlists/rockyou.txt ssh://192.168.1.3:2222

# 3. Web Scan
nikto -h http://192.168.1.3:8080
```

#### **H. مراقبة النتائج**
```
ارجع لـ Windows وشاهد:
• Dashboard: http://localhost:8501
• Grafana: http://localhost:3000
• اللوجز في: A:\cyber_mirage\data\logs\
```

---

## 📊 **مقارنة الطرق**

```
╔════════════════════════════════════════════════════════════╗
║  الطريقة         السهولة    الميزات    الاستخدام      ║
╠════════════════════════════════════════════════════════════╣
║  1. بسيط         ⭐⭐⭐⭐⭐   ⭐⭐        اختبار سريع  ║
║  2. Docker       ⭐⭐⭐       ⭐⭐⭐⭐⭐  Production  ║
║  3. Attack Test  ⭐⭐         ⭐⭐⭐⭐    Testing     ║
╚════════════════════════════════════════════════════════════╝
```

---

## 🎯 **التوصية حسب الهدف**

### **إذا كنت عايز:**

#### **✅ تجربة سريعة (5 دقائق):**
```powershell
.\start_defense.ps1
# ثم افتح: http://localhost:8501
```

#### **✅ استخدام فعلي (Production):**
```powershell
# 1. حمّل Docker Desktop
# 2. .\deploy_production.ps1 -Action start
# 3. افتح: http://localhost:8501 و http://localhost:3000
```

#### **✅ اختبار الهجمات:**
```
# 1. شغّل System (الطريقة 1 أو 2)
# 2. حمّل Kali Linux VM
# 3. نفّذ الهجمات
# 4. راقب النتائج في Dashboard
```

---

## 🔧 **استكشاف المشاكل**

### **مشكلة: Port Already in Use**
```powershell
# ابحث عن البروسيس
netstat -ano | findstr :8501

# أغلق البروسيس
taskkill /PID <PID> /F
```

### **مشكلة: Docker لا يعمل**
```
1. تأكد إن Docker Desktop مفتوح
2. انتظر حتى يظهر "Docker Desktop is running"
3. جرب الأمر:
   docker ps
```

### **مشكلة: Module not found**
```powershell
# أعد تثبيت المكتبات
.\venv\Scripts\python.exe -m pip install -r requirements.txt
```

### **مشكلة: Virtual Environment لا يعمل**
```powershell
# أعد إنشائه
python -m venv venv
.\venv\Scripts\Activate.ps1
pip install -r requirements.txt
```

---

## 📋 **Checklist للتشغيل الأول**

### **الطريقة 1 (بسيط):**
```
□ افتح PowerShell في A:\cyber_mirage
□ شغّل: .\start_defense.ps1
□ انتظر فتح 3 نوافذ
□ افتح http://localhost:8501
□ ✅ جاهز!
```

### **الطريقة 2 (Docker):**
```
□ حمّل Docker Desktop (مرة واحدة)
□ شغّل Docker Desktop
□ انسخ .env: Copy-Item .env.example .env
□ عدّل الباسوردات في .env
□ شغّل: .\deploy_production.ps1 -Action start
□ انتظر 2-3 دقائق
□ تحقق: .\deploy_production.ps1 -Action status
□ افتح http://localhost:8501
□ افتح http://localhost:3000 (Grafana)
□ ✅ جاهز!
```

### **الطريقة 3 (Attack Testing):**
```
□ شغّل System (الطريقة 1 أو 2)
□ حمّل VirtualBox
□ حمّل Kali Linux OVA
□ استورد Kali في VirtualBox
□ غيّر Network إلى Bridged
□ شغّل Kali VM
□ اختبر ping 192.168.1.3
□ شغّل nmap أو hydra
□ راقب النتائج في Dashboard
□ ✅ جاهز!
```

---

## 💡 **نصائح مهمة**

### **للمبتدئين:**
```
✅ ابدأ بالطريقة 1 (الأسهل)
✅ اختبر Dashboard أولاً
✅ اقرأ QUICKSTART.md
✅ جرب run_benchmarks.ps1
✅ اختبر Unit Tests
```

### **للاستخدام الفعلي:**
```
✅ استخدم الطريقة 2 (Docker)
✅ غيّر كل الباسوردات في .env
✅ راقب Grafana بانتظام
✅ اعمل backup للـ database
✅ اقرأ DOCKER_PRODUCTION_GUIDE.md
```

### **للاختبار المتقدم:**
```
✅ استخدم الطريقة 3 (Attack Testing)
✅ جرب أنواع هجمات مختلفة
✅ راقب AI responses
✅ حلل اللوجز
✅ اقرأ ATTACKER_GUIDE.md
```

---

## 📚 **الملفات المفيدة**

### **Quick Reference:**
```
📖 QUICKSTART.md                  - بداية سريعة
📖 DOCKER_PRODUCTION_GUIDE.md     - دليل Docker (800 lines)
📖 PROJECT_100_COMPLETE.md        - الملخص النهائي
📖 UNIT_TESTS_COMPLETE.md         - دليل الاختبارات
📖 ATTACKER_GUIDE.md              - دليل الهجمات
```

### **Scripts:**
```
🔧 start_defense.ps1              - تشغيل بسيط
🔧 deploy_production.ps1          - تشغيل Docker
🔧 run_benchmarks.ps1             - اختبار الأداء
🔧 tests/run_all_tests.py         - اختبار الكود
```

---

## 🎓 **أوامر سريعة**

### **تشغيل:**
```powershell
# بسيط
.\start_defense.ps1

# Docker
.\deploy_production.ps1 -Action start

# اختبار
.\venv\Scripts\python.exe tests/run_all_tests.py
```

### **مراقبة:**
```powershell
# حالة Docker
.\deploy_production.ps1 -Action status

# اللوجز
.\deploy_production.ps1 -Action logs

# اللوجز لخدمة معينة
.\deploy_production.ps1 -Action logs -Service ai-engine
```

### **إيقاف:**
```powershell
# إيقاف Docker
.\deploy_production.ps1 -Action stop

# إيقاف بسيط
# أغلق النوافذ أو Ctrl+C
```

---

## 🚀 **البداية الموصى بها**

### **اليوم الأول (30 دقيقة):**
```powershell
# 1. تشغيل بسيط
.\start_defense.ps1

# 2. افتح Dashboard
# http://localhost:8501

# 3. استكشف الواجهة
```

### **اليوم الثاني (1 ساعة):**
```powershell
# 1. حمّل Docker Desktop
# 2. ثبّته وأعد التشغيل
# 3. شغّل Production
.\deploy_production.ps1 -Action start

# 4. استكشف Grafana
# http://localhost:3000
```

### **اليوم الثالث (2-3 ساعات):**
```
# 1. حمّل Kali Linux
# 2. ثبّت VirtualBox
# 3. استورد Kali
# 4. نفّذ هجمات تجريبية
# 5. حلّل النتائج
```

---

## ✅ **أنت جاهز الآن!**

```
╔════════════════════════════════════════════════════════════╗
║  🎯 ابدأ الآن!                                            ║
╠════════════════════════════════════════════════════════════╣
║  للبداية السريعة:                                        ║
║  1. افتح PowerShell                                       ║
║  2. cd A:\cyber_mirage                                    ║
║  3. .\start_defense.ps1                                   ║
║  4. افتح http://localhost:8501                            ║
║                                                            ║
║  للـ Production:                                          ║
║  1. حمّل Docker Desktop                                   ║
║  2. .\deploy_production.ps1 -Action start                 ║
║  3. افتح http://localhost:8501 و :3000                    ║
╚════════════════════════════════════════════════════════════╝
```

---

**تم إعداده:** 27 أكتوبر 2025  
**النسخة:** 5.0 LEGENDARY  
**الحالة:** 100% Complete ✅

**🎉 بالتوفيق! 🎉**
