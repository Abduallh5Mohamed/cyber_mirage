# 📊 Grafana و Prometheus - شرح شامل

## 🎯 الفكرة الأساسية

### Prometheus و Grafana شغل "ثنائي":

```
┌─────────────────────────────────────────────┐
│                                             │
│   Prometheus يجمع البيانات                │
│   (مثل جهاز التسجيل)                       │
│                                             │
│   ⬇️⬇️⬇️⬇️⬇️                               │
│                                             │
│   Grafana يعرض البيانات                   │
│   (مثل التلفزيون)                         │
│                                             │
└─────────────────────────────────────────────┘
```

---

## 📈 **ما هو Prometheus؟**

### التعريف البسيط:
**Prometheus = جهاز مراقبة / مسجّل بيانات**

### الوظيفة:
- ✅ يجمع البيانات من الأنظمة
- ✅ يخزنها في قاعدة بيانات
- ✅ يحفظها في ملفات

### مثال من الحياة الواقعية:
```
Prometheus = محطة الأرصاد الجوية

تجمع بيانات مثل:
- درجة الحرارة ✓
- الرطوبة ✓
- سرعة الريح ✓
- الضغط ✓

وتحفظها في سجلات!
```

### البيانات اللي جمعها Prometheus:

1. **استهلاك الـ CPU**
   ```
   cpu_usage = 45%
   cpu_usage = 47%
   cpu_usage = 42%
   ```

2. **استخدام الذاكرة**
   ```
   memory_usage = 1.2 GB
   memory_usage = 1.3 GB
   memory_usage = 1.1 GB
   ```

3. **عدد الطلبات**
   ```
   http_requests = 100
   http_requests = 120
   http_requests = 95
   ```

4. **حالة الخدمات**
   ```
   redis_up = 1 (تشغيل)
   postgres_up = 1 (تشغيل)
   ```

---

## 🎨 **ما هو Grafana؟**

### التعريف البسيط:
**Grafana = واجهة عرض بيانات جميلة**

### الوظيفة:
- ✅ يأخذ البيانات من Prometheus
- ✅ يرسمها في رسوم بيانية
- ✅ يعرضها بشكل جميل
- ✅ يعطيك تنبيهات

### مثال من الحياة الواقعية:
```
Grafana = شاشة التلفزيون

تأخذ بيانات محطة الأرصاد وتعرضها:
- رسم بياني للحرارة
- خريطة الرياح
- توقعات الطقس

بشكل جميل وسهل الفهم!
```

### لوحات Grafana:
```
Dashboard 1: استخدام النظام
├─ رسم CPU
├─ رسم الذاكرة
├─ رسم القرص الصلب
└─ رسم الشبكة

Dashboard 2: الخدمات
├─ حالة Redis
├─ حالة PostgreSQL
├─ حالة API
└─ حالة Dashboard

Dashboard 3: الأمان
├─ عدد الهجمات
├─ أنواع الهجمات
├─ مصدر الهجمات
└─ حالة الدفاع
```

---

## 🔄 **كيفية العمل معاً؟**

### المراحل:

```
1️⃣ Prometheus يجمع البيانات
   └─ كل 15 ثانية يأخذ قراءة

2️⃣ Prometheus يخزنها
   └─ في قاعدة بيانات TSDB

3️⃣ Grafana تقرأها
   └─ كل دقيقة

4️⃣ Grafana ترسمها
   └─ في رسوم بيانية جميلة

5️⃣ أنت تشوفها
   └─ في المتصفح! 🎨
```

---

## 🌍 **الاختلافات بينهم:**

| الميزة | Prometheus | Grafana |
|--------|-----------|---------|
| **الوظيفة** | جمع البيانات | عرض البيانات |
| **قاعدة البيانات** | ✅ نعم (TSDB) | ❌ لا (تستخدم Prometheus) |
| **الواجهة** | ❌ بسيطة جداً | ✅ جميلة جداً |
| **التنبيهات** | ✅ نعم | ✅ نعم |
| **الرسوم البيانية** | ❌ أساسية | ✅ متقدمة جداً |
| **الوصول** | http://localhost:9090 | http://localhost:3000 |

---

## 💡 **لماذا نحتاجهما؟**

### مثال عملي من مشروعك (Cyber Mirage):

#### بدون Prometheus و Grafana:
```
❌ ما تعرف إذا هجوم حصل
❌ ما تعرف كمية الموارد المستخدمة
❌ ما تعرف إذا الخدمات تمام
❌ ما تعرف الأداء الكلي
```

#### مع Prometheus و Grafana:
```
✅ شوف عدد الهجمات في الوقت الفعلي
✅ شوف استخدام CPU والذاكرة
✅ شوف حالة كل خدمة (Redis, PostgreSQL)
✅ شوف أداء الـ API
✅ اعرف متى حصلت مشاكل
```

---

## 📊 **أمثلة على البيانات اللي جمعها:**

### في Cyber Mirage:

#### 1. عدد الهجمات المكتشفة:
```
Prometheus tracks:
- attacks_detected_total = 245
- attacks_by_type: SQL Injection = 120
- attacks_by_type: XSS = 85
- attacks_by_type: DoS = 40
```

#### 2. أداء النظام:
```
Prometheus tracks:
- http_request_duration = 45ms
- model_inference_time = 120ms
- database_query_time = 25ms
- api_response_time = 90ms
```

#### 3. استخدام الموارد:
```
Prometheus tracks:
- container_cpu_usage = 12%
- container_memory_usage = 512MB
- container_disk_usage = 50GB
- network_bandwidth = 5MB/s
```

#### 4. حالة الخدمات:
```
Prometheus tracks:
- redis_up = 1 (✅ تشغيل)
- postgres_up = 1 (✅ تشغيل)
- prometheus_up = 1 (✅ تشغيل)
- api_up = 1 (✅ تشغيل)
```

---

## 🎯 **كيفية استخدام Prometheus؟**

### افتح الرابط:
```
http://localhost:9090
```

### اكتب استعلام بسيط:
```
# مثال 1: عرض استهلاك CPU
container_cpu_usage_seconds_total

# مثال 2: عرض استخدام الذاكرة
container_memory_usage_bytes

# مثال 3: عرض أخطاء النظام
errors_total

# مثال 4: عرض الهجمات
attacks_detected_total
```

### الرسم البياني:
- ستشوف خط يمثل البيانات مع الوقت
- يتحدث كل 15 ثانية
- يمكنك تغيير الوقت

---

## 🎨 **كيفية استخدام Grafana؟**

### الخطوة 1: افتح الرابط
```
http://localhost:3000

Username: admin
Password: admin
```

### الخطوة 2: اختر Dashboard
```
Dashboards → Browse
```

### الخطوة 3: شوف البيانات
```
- رسوم بيانية جميلة
- معلومات فورية
- تنبيهات (إذا كانت موجودة)
```

### الخطوة 4: اعمل Dashboard خاص بك
```
+ Create New Dashboard
→ Add Panel
→ اختر Prometheus كـ Data Source
→ اكتب استعلام
→ اختر نوع الرسم (خط، بار، إلخ)
→ احفظ
```

---

## 🔧 **الإعدادات:**

### إضافة Prometheus إلى Grafana:

1. افتح Grafana → Configuration → Data Sources
2. اضغط "Add data source"
3. اختر Prometheus
4. أدخل URL: `http://prometheus:9090`
5. اضغط "Save & Test"

### استيراد Dashboard جاهز:

1. Dashboards → Import
2. أدخل ID من https://grafana.com/grafana/dashboards
3. اختر Prometheus كـ Data Source
4. اضغط Import

---

## 📋 **استعلامات مفيدة:**

### CPU:
```
rate(container_cpu_usage_seconds_total[5m])
```

### الذاكرة:
```
container_memory_usage_bytes / 1024 / 1024
```

### الشبكة:
```
rate(container_network_receive_bytes_total[5m])
```

### الأخطاء:
```
errors_total
```

### الهجمات:
```
attacks_detected_total
```

---

## 🎯 **الفرق البسيط جداً:**

### Prometheus:
```
الشيف اللي بيطبخ البيانات ⚙️
```

### Grafana:
```
الطبق اللي بتقدم فيه الأكلة 🍽️
```

---

## ✨ **المميزات:**

### Prometheus:
✅ دقيق جداً (قراءات كل 15 ثانية)
✅ يخزن بيانات طويلة الأمد
✅ استعلامات قوية جداً
✅ تنبيهات مرنة
✅ مجاني وopen-source

### Grafana:
✅ واجهة جميلة جداً
✅ رسوم بيانية احترافية
✅ dashboards متعددة
✅ مشاركة سهلة
✅ سهل جداً للمبتدئين

---

## 🎓 **هل أحتاجهما بالفعل؟**

### نعم! إذا كنت تريد:

✅ معرفة أداء النظام
✅ اكتشاف المشاكل مبكراً
✅ مراقبة الخدمات
✅ فهم سلوك النظام
✅ تحسين الأداء
✅ اكتشاف الهجمات
✅ توثيق البيانات

### لا! إذا:

❌ تريد شيء بسيط جداً فقط
❌ ما تحتاج مراقبة فعلية

---

## 🚀 **ملخص سريع:**

```
Prometheus = يجمع البيانات 📊
Grafana = يعرضها بشكل جميل 🎨

معاً = نظام مراقبة قوي جداً! 💪
```

---

## 🎉 **الخلاصة:**

| السؤال | الإجابة |
|--------|---------|
| **إيه Prometheus؟** | جهاز جمع بيانات من الأنظمة |
| **إيه Grafana؟** | واجهة عرض البيانات بشكل جميل |
| **ليه نحتاجهما؟** | للمراقبة الفعلية والأداء |
| **لازم يشتغلوا معاً؟** | أيوه! Grafana تحتاج Prometheus |
| **الرابط؟** | Prometheus: 9090, Grafana: 3000 |
| **سهلين؟** | نعم جداً! بسيطين جداً! |

---

## 📞 **معلومات إضافية:**

### الموارد الرسمية:
- Prometheus: https://prometheus.io
- Grafana: https://grafana.com

### الاستخدام في مشروعك:
- تراقب الهجمات المكتشفة
- تراقب أداء الـ AI
- تراقب استهلاك الموارد
- تراقب حالة الخدمات

---

**الآن تعرف كل شيء عن Prometheus و Grafana! 🎊**

استمتع بالمراقبة! 📊🎨
