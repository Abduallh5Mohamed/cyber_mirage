# 🐳 دليل Docker السريع - خطوة بخطوة

## ✅ الحالة الحالية:
```
✅ Docker 28.4.0 مثبت
✅ Docker Compose 2.39.4 مثبت
✅ Docker Daemon شغال
✅ ملف .env تم إنشاؤه
```

---

## 🚀 الخطوات (5 دقائق):

### **خطوة 1: تعديل الباسوردات** ⚠️ مهم جداً!

```powershell
# افتح ملف .env
notepad .env
```

**غيّر هذه الأسطر:**
```bash
# ════════════════════════════════════════
# غيّر الباسوردات دي بباسوردات قوية
# ════════════════════════════════════════

# Database Passwords
POSTGRES_PASSWORD=YourSecurePassword123!     # غيّره!
REDIS_PASSWORD=YourRedisPassword456!         # غيّره!

# Grafana Password
GRAFANA_PASSWORD=admin123                    # غيّره!
GRAFANA_SECRET=your-random-secret-key        # غيّره!

# API Keys (اختياري - ممكن تسيبهم فاضيين)
VIRUSTOTAL_API_KEY=
ABUSEIPDB_API_KEY=
SHODAN_API_KEY=
```

**مثال باسوردات قوية:**
```bash
POSTGRES_PASSWORD=Cyber@Mirage#2025!Secure
REDIS_PASSWORD=Redis$Strong*Pass#789
GRAFANA_PASSWORD=Grafana!Admin@456
GRAFANA_SECRET=a8f3k2m9p1x5z7c4v6b8n0q3w1e5r7t9
```

**💡 نصيحة:** استخدم باسوردات طويلة (16+ حرف) فيها:
- ✅ حروف كبيرة A-Z
- ✅ حروف صغيرة a-z  
- ✅ أرقام 0-9
- ✅ رموز !@#$%^&*

ثم **احفظ الملف** (Ctrl+S) وأغلقه.

---

### **خطوة 2: تشغيل Production Stack** 🚀

```powershell
.\deploy_production.ps1 -Action start
```

**ماذا سيحدث؟**
```
⏳ سيبدأ تحميل وتشغيل 10 خدمات:
   1. AI Engine         (ذكاء اصطناعي)
   2. Dashboard         (لوحة التحكم)
   3. Honeypots         (الفخاخ)
   4. Redis             (قاعدة بيانات)
   5. PostgreSQL        (قاعدة بيانات)
   6. Prometheus        (مراقبة)
   7. Grafana           (لوحات)
   8. Alertmanager      (تنبيهات)
   9. Node Exporter     (مراقبة النظام)
   10. cAdvisor         (مراقبة الحاويات)

⏱️ الوقت المتوقع: 2-3 دقائق (أول مرة قد تأخذ 5-10 دقائق)
```

**انتظر حتى ترى:**
```
✅ Stack started successfully!
```

---

### **خطوة 3: التحقق من الحالة** ✅

```powershell
.\deploy_production.ps1 -Action status
```

**يجب أن ترى:**
```
NAME                          STATUS              PORTS
cyber_mirage_ai              Up (healthy)        8001-8003
cyber_mirage_dashboard       Up (healthy)        8501
cyber_mirage_honeypots       Up (healthy)        Multiple
cyber_mirage_redis           Up (healthy)        6379
cyber_mirage_postgres        Up (healthy)        5432
cyber_mirage_prometheus      Up                  9090
cyber_mirage_grafana         Up                  3000
...
```

✅ **إذا ظهر "Up" أو "Up (healthy)" → كل شيء شغال!**

---

### **خطوة 4: افتح الخدمات** 🌐

**في المتصفح، افتح:**

#### **1. Dashboard (لوحة التحكم الرئيسية):**
```
http://localhost:8501
```
- الواجهة الرئيسية
- مراقبة الهجمات في الوقت الفعلي
- إحصائيات AI

#### **2. Grafana (لوحات المراقبة):**
```
http://localhost:3000

Username: admin
Password: (الباسورد اللي حطيته في .env)
```
- dashboards احترافية
- رسوم بيانية
- تتبع الأداء

#### **3. Prometheus (المقاييس):**
```
http://localhost:9090
```
- مقاييس تفصيلية
- queries متقدمة
- بيانات خام

#### **4. Alertmanager (التنبيهات):**
```
http://localhost:9093
```
- إدارة التنبيهات
- تتبع المشاكل

---

## 🎯 أوامر مفيدة:

### **مشاهدة اللوجز:**
```powershell
# كل اللوجز
.\deploy_production.ps1 -Action logs

# لوج خدمة معينة
.\deploy_production.ps1 -Action logs -Service ai-engine
.\deploy_production.ps1 -Action logs -Service dashboard
.\deploy_production.ps1 -Action logs -Service honeypots
```

### **إعادة التشغيل:**
```powershell
.\deploy_production.ps1 -Action restart
```

### **الإيقاف:**
```powershell
.\deploy_production.ps1 -Action stop
```

### **إعادة البناء (بعد تغيير الكود):**
```powershell
.\deploy_production.ps1 -Action rebuild
```

### **التنظيف الكامل** ⚠️ (يحذف كل البيانات):
```powershell
.\deploy_production.ps1 -Action clean
```

---

## 🔍 استكشاف المشاكل:

### **مشكلة: خدمة لا تعمل**
```powershell
# شاهد اللوج
.\deploy_production.ps1 -Action logs -Service اسم_الخدمة

# أعد تشغيل الخدمة
docker restart cyber_mirage_اسم_الخدمة
```

### **مشكلة: Port Already in Use**
```powershell
# ابحث عن البروسيس
netstat -ano | findstr :8501

# أغلق البروسيس
taskkill /PID <رقم_البروسيس> /F
```

### **مشكلة: Out of Memory**
```
حل: 
1. افتح Docker Desktop
2. Settings → Resources
3. زود Memory إلى 8GB أو أكثر
4. اضغط Apply & Restart
```

### **مشكلة: Container Unhealthy**
```powershell
# أعد تشغيل الحاوية
docker restart cyber_mirage_اسم_الخدمة

# إذا استمرت المشكلة، أعد بناء كل شيء
.\deploy_production.ps1 -Action rebuild
```

---

## 📊 مراقبة الأداء:

### **في Docker Desktop:**
```
1. افتح Docker Desktop
2. اذهب إلى "Containers"
3. شاهد:
   • CPU Usage
   • Memory Usage
   • Network Traffic
```

### **في Terminal:**
```powershell
# إحصائيات حية
docker stats

# إحصائيات لحظية
docker stats --no-stream
```

---

## 🎓 ماذا بعد؟

### **1. استكشف Dashboard:**
```
http://localhost:8501
```
- شاهد الإحصائيات
- راقب الهجمات
- تفاعل مع AI

### **2. تعلم Grafana:**
```
http://localhost:3000
```
- استكشف الـ dashboards
- أنشئ dashboard خاص بك
- ضبط التنبيهات

### **3. اختبر Honeypots:**
```bash
# من جهاز آخر أو Kali Linux
ssh admin@192.168.1.3 -p 2222
```
- راقب الهجمات في Dashboard
- شاهد AI responses
- حلل اللوجز

### **4. تعديل الإعدادات:**
```powershell
# عدّل docker-compose.production.yml
notepad docker-compose.production.yml

# ثم أعد البناء
.\deploy_production.ps1 -Action rebuild
```

---

## 🔐 نصائح الأمان:

```
✅ غيّر كل الباسوردات في .env
✅ لا تشارك ملف .env مع أحد
✅ استخدم باسوردات قوية (16+ حرف)
✅ فعّل TLS في الإنتاج
✅ راجع الـ alerts بانتظام
✅ اعمل backup للـ database
✅ حدّث الـ images بانتظام
```

---

## 📈 مقاييس النجاح:

```
╔════════════════════════════════════════════════════════════╗
║  ✅ كل الخدمات "Up" في status                            ║
║  ✅ Dashboard يفتح على localhost:8501                     ║
║  ✅ Grafana يفتح على localhost:3000                       ║
║  ✅ لا توجد errors في اللوجز                             ║
║  ✅ CPU < 80%                                             ║
║  ✅ Memory < 85%                                          ║
╚════════════════════════════════════════════════════════════╝
```

---

## 🎯 الخطوات مرة أخرى:

```powershell
# 1. عدّل الباسوردات
notepad .env

# 2. شغّل
.\deploy_production.ps1 -Action start

# 3. تحقق
.\deploy_production.ps1 -Action status

# 4. افتح المتصفح
# http://localhost:8501
# http://localhost:3000

# ✅ Done!
```

---

## 📞 للمساعدة:

### **الوثائق:**
- `DOCKER_PRODUCTION_GUIDE.md` - دليل كامل (800 سطر)
- `HOW_TO_START.md` - دليل البداية
- `START_HERE.md` - ملخص سريع

### **الأوامر السريعة:**
```powershell
# Start
.\deploy_production.ps1 -Action start

# Stop
.\deploy_production.ps1 -Action stop

# Status
.\deploy_production.ps1 -Action status

# Logs
.\deploy_production.ps1 -Action logs
```

---

**🎉 بالتوفيق! Docker Production جاهز! 🎉**

```
╔════════════════════════════════════════════════════════════╗
║  🐳 DOCKER PRODUCTION - READY TO GO!                      ║
╠════════════════════════════════════════════════════════════╣
║  Next Command:                                            ║
║  .\deploy_production.ps1 -Action start                    ║
╚════════════════════════════════════════════════════════════╝
```
