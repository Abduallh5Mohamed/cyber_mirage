# 🎨 الخريطة البصرية للنظام

## 🗺️ معمارية النظام الكاملة

```
┌─────────────────────────────────────────────────────────────────────┐
│                         جهاز المستخدم                              │
│                      (Windows 11 + Chrome)                         │
└─────────────────────────────────────────────────────────────────────┘
        │                                    │                      │
        ▼                                    ▼                      ▼
┌──────────────────┐  ┌──────────────────┐  ┌──────────────────┐
│  Dashboard       │  │   Prometheus     │  │    Grafana       │
│  Streamlit       │  │   http://9090    │  │ http://localhost │
│  8501            │  │                  │  │     :3000        │
│  (Python)        │  │  (جمع البيانات) │  │ (عرض البيانات)  │
└────────┬─────────┘  └────────┬─────────┘  └────────┬─────────┘
         │                     │                      │
         │                     │                      │
         └─────────────────┬───┴──────────────────────┘
                           │
                    ┌──────▼──────┐
                    │  Docker     │
                    │  Network    │
                    │             │
                    └──────┬──────┘
        ┌───────────────────┼───────────────────┬─────────────────┐
        │                   │                   │                 │
        ▼                   ▼                   ▼                 ▼
   ┌────────────┐  ┌────────────┐  ┌──────────────┐  ┌──────────────┐
   │   Redis    │  │ PostgreSQL │  │ Node         │  │  Honeypot    │
   │ 6379       │  │ 5432       │  │  Exporter    │  │  API 8080    │
   │            │  │            │  │ 9100         │  │              │
   │ (Cache)    │  │(قاعدة رئ)  │  │(قراءة نظام)  │  │(Python API)  │
   └────────────┘  └────────────┘  └──────────────┘  └──────────────┘
        │                │                │
        │                │                │
        └────────────────┴────────────────┘
                         │
                ┌────────▼────────┐
                │   البيانات      │
                │   والسجلات      │
                │   في الذاكرة     │
                └─────────────────┘
```

---

## 🔄 تدفق البيانات

```
1. الهجوم يحصل
   │
   ▼
2. Honeypot API يكتشفه
   ├─ يحفظ في PostgreSQL (دائم) 💾
   ├─ يحفظ في Redis (مؤقت) ⚡
   └─ يرسل metric إلى Prometheus 📊
   │
   ▼
3. Node Exporter يقيس النظام
   └─ CPU, RAM, Network → Prometheus
   │
   ▼
4. Prometheus يجمع البيانات
   └─ كل 15 ثانية
   │
   ▼
5. Grafana تقرأ من Prometheus
   └─ تحدّث الرسوم البيانية
   │
   ▼
6. أنت تشوف النتائج في Dashboard
   ├─ Grafana (رسوم بيانية) 📊
   ├─ Prometheus (بيانات خام) 📋
   └─ Streamlit Dashboard (واجهة) 🎨
```

---

## 🏗️ الطبقات المعمارية

```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│              طبقة العرض (Presentation Layer)               │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ Streamlit Dashboard (8501)  │ Grafana (3000)        │  │
│  │ واجهة جميلة للمستخدم        │ لوحة تحكم احترافية   │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                             │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│            طبقة جمع المراقبة (Monitoring Layer)            │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ Prometheus (9090)      │ Node Exporter (9100)       │  │
│  │ جمع البيانات الذكي     │ قراءة النظام تلقائياً     │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                             │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│              طبقة التطبيق (Application Layer)              │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ Honeypot API (8080)                                │  │
│  │ - يكتشف الهجمات                                    │  │
│  │ - يرسل التنبيهات                                  │  │
│  │ - يحفظ البيانات                                   │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                             │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│              طبقة التخزين (Storage Layer)                  │
│  ┌──────────────────────────────────────────────────────┐  │
│  │ PostgreSQL     │ Redis      │ Prometheus TSDB       │  │
│  │ (5432)         │ (6379)     │ (الوقت الفعلي)       │  │
│  │ تخزين دائم     │ كاش سريع   │ سلاسل زمنية        │  │
│  └──────────────────────────────────────────────────────┘  │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

---

## 🔌 الاتصالات بين الخدمات

```
                    Streamlit Dashboard
                           │
                           ▼
        ┌──────────────────────────────────┐
        │      Docker Network Bridge       │
        │   (cyber_mirage_honeypot_net)   │
        │                                  │
        │  ┌────────────────────────────┐  │
        │  │  Honeypot API              │  │
        │  │  ├─→ PostgreSQL (writes)   │  │
        │  │  ├─→ Redis (cache)         │  │
        │  │  └─→ Prometheus (metrics)  │  │
        │  └────────────────────────────┘  │
        │           │       │       │       │
        │  ┌────────▼───┐  ▼      ▼  ┌─────┴────────┐
        │  │ PostgreSQL │         ┌──┤ Prometheus   │
        │  │ :5432      │  Redis  │  │ :9090        │
        │  │            │  :6379  │  │  ◄─ Node     │
        │  │  (DB)      │  (Cache)│  │     Exporter │
        │  └────────────┘         │  │  :9100       │
        │                         └──┤              │
        │                            │  ◄─ Grafana │
        │                            │     :3000    │
        │                            └──────────────┘
        └────────────────────────────────┘
```

---

## 📊 البيانات في كل خدمة

```
┌────────────────────────────────────────────────────────┐
│                   PostgreSQL                          │
│              (التخزين الدائم)                         │
│                                                        │
│  Table: attacks                                       │
│  ├─ id: 1, type: SQL, ip: 192.168.1.1               │
│  ├─ id: 2, type: XSS, ip: 192.168.1.2               │
│  └─ id: 3, type: DoS, ip: 192.168.1.3               │
│                                                        │
│  Table: users                                         │
│  ├─ id: 1, username: admin, role: superuser          │
│  └─ id: 2, username: analyst, role: analyst          │
│                                                        │
│  محفوظ للأبد! 💾                                      │
└────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────┐
│                    Redis                              │
│               (التخزين المؤقت)                        │
│                                                        │
│  session:user123 → {token: "xyz", expires: 3600}     │
│  cache:dashboard → {data: [...], ttl: 300}            │
│  queue:alerts → [alert1, alert2, alert3]             │
│                                                        │
│  سريع جداً ⚡ لكن مؤقت!                              │
└────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────┐
│              Prometheus (Time Series DB)              │
│                                                        │
│  attacks_total = [245, 245, 246, 247, 247, 248]      │
│  cpu_usage = [45%, 46%, 47%, 45%, 44%, 46%]          │
│  memory_usage = [512MB, 520MB, 515MB, ...]           │
│                                                        │
│  مع الوقت والتاريخ الدقيق! 📊                       │
└────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────┐
│                    Grafana                            │
│                                                        │
│  ┌──────────────────┐  ┌──────────────────┐          │
│  │ Attacks Panel    │  │ CPU Usage Gauge  │          │
│  │  ▂▃▄▅▆▇█▇▆▅▄▃▂  │  │   ┌──────────┐   │          │
│  │  245 attacks     │  │   │   45%    │   │          │
│  └──────────────────┘  │   └──────────┘   │          │
│                        └──────────────────┘          │
│  جميل وسهل الفهم! 🎨                                │
└────────────────────────────────────────────────────────┘
```

---

## 🔀 مسارات البيانات المختلفة

### المسار 1: الهجوم
```
Hacker → Network → Honeypot API → Detection
                       ├─→ PostgreSQL (save)
                       ├─→ Redis (cache)
                       └─→ Prometheus (metric)
```

### المسار 2: المراقبة
```
Node Exporter → Prometheus ← Grafana ← Browser
(قراءات)      (جمع)      (عرض)
```

### المسار 3: العرض
```
PostgreSQL → API → Streamlit Dashboard → Browser
(البيانات)  (معالجة)  (واجهة)
```

---

## 🎯 تدفق الطلب (Request Flow)

### مثال: طلب إحصائيات الهجمات

```
Step 1: المستخدم يفتح Streamlit Dashboard
  → http://localhost:8501
  
Step 2: Dashboard يطلب الإحصائيات
  → API.get("/statistics")
  
Step 3: API يسأل PostgreSQL
  → SELECT COUNT(*) FROM attacks
  → النتيجة: 245
  
Step 4: API يأخذ من Redis (كاش)
  → cache:stats → {attacks: 245}
  
Step 5: Dashboard يعرض النتيجة
  → رسم بياني: Total Attacks: 245

┌─────────┐  ┌─────┐  ┌────────────┐  ┌──────┐  ┌───────┐
│ Browser │→ │ API │→ │ PostgreSQL │  │ Redis│  │Display│
└─────────┘  └─────┘  └────────────┘  └──────┘  └───────┘
     ▲         │            ▲
     └─────────┴────────────┘
         JSON Response
```

---

## 🌐 الشبكة

```
┌──────────────────────────────────────────────────┐
│           Docker Bridge Network                 │
│    (cyber_mirage_honeypot_network)              │
│                                                 │
│  Gateway: 172.20.0.1                           │
│                                                 │
│  ┌────────────────────────────────────────┐    │
│  │ Container IPs:                         │    │
│  │                                        │    │
│  │ api:       172.20.0.2                 │    │
│  │ redis:     172.20.0.3                 │    │
│  │ postgres:  172.20.0.4                 │    │
│  │ prometheus: 172.20.0.5                │    │
│  │ grafana:   172.20.0.6                 │    │
│  │ node-exp:  172.20.0.7                 │    │
│  │                                        │    │
│  └────────────────────────────────────────┘    │
│                                                 │
│  الاتصالات: درع من الحماية! 🛡️               │
│  (معزولة عن جهازك المباشر)                    │
└──────────────────────────────────────────────────┘
```

---

## 💾 مكان التخزين

```
جهازك
  │
  └─ A:\cyber_mirage
      ├─ docker-compose.yml
      ├─ src/
      ├─ data/
      │  ├─ logs/ (سجلات الـ API)
      │  └─ models/
      └─ volumes/ (بيانات Docker)
         ├─ redis_data/ (قاعدة Redis)
         ├─ postgres_data/ (قاعدة PostgreSQL)
         ├─ prometheus_data/ (قاعدة Prometheus)
         └─ grafana_data/ (إعدادات Grafana)
```

---

## ⚡ السرعات والأداء

```
Prometheus queries:  ~50ms
Grafana render:      ~200ms
PostgreSQL queries:  ~10ms
Redis queries:       ~1ms
API response:        ~100ms
Network latency:     <1ms (Docker bridge)

┌───────────────────────────────────────┐
│ Speed Comparison:                     │
│                                       │
│ Redis      ████ 1ms                  │
│ PostgreSQL ████████ 10ms             │
│ API        ██████████████ 100ms      │
│ Prometheus ████████████████ 50ms     │
│ Grafana    ████████████████████ 200ms│
│                                       │
│ الكل سريع! ⚡                        │
└───────────────────────────────────────┘
```

---

## 🎯 الملخص البصري

```
جهازك (Windows)
    │
    ├─ Port 8501  ← Streamlit Dashboard (Python)
    ├─ Port 8080  ← Honeypot API (FastAPI)
    ├─ Port 3000  ← Grafana (Docker)
    ├─ Port 9090  ← Prometheus (Docker)
    ├─ Port 6379  ← Redis (Docker)
    ├─ Port 5432  ← PostgreSQL (Docker)
    └─ Port 9100  ← Node Exporter (Docker)
    
    كل شيء متصل وشغّال معاً! 🎉
```

---

**الآن أنت تفهم البنية الكاملة للنظام! 🎊**
