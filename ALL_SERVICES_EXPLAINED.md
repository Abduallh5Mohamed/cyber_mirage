# 🗂️ شرح جميع الخدمات - الفروقات والاستخدامات

## 🎯 الخدمات الشغالة دلوقتي (5 خدمات):

```
1. Redis ......................... قاعدة بيانات سريعة
2. PostgreSQL ..................... قاعدة بيانات رئيسية
3. Prometheus ..................... جمع البيانات
4. Grafana ........................ عرض البيانات
5. Node Exporter .................. قراءة بيانات النظام
```

---

## 📊 **الجدول المقارن الشامل:**

| الخدمة | الغرض | المنفذ | النوع | الاستخدام |
|--------|-------|--------|-------|-----------|
| **Redis** | تخزين سريع | 6379 | قاعدة بيانات | الكاش وجلسات المستخدمين |
| **PostgreSQL** | تخزين دائم | 5432 | قاعدة بيانات | البيانات الهامة |
| **Prometheus** | جمع البيانات | 9090 | مراقبة | تسجيل الأداء والصحة |
| **Grafana** | عرض البيانات | 3000 | لوحة تحكم | عرض رسوم بيانية |
| **Node Exporter** | قراءة النظام | 9100 | مراقبة | قراءة CPU والذاكرة |

---

## 🔴 **Redis - قاعدة البيانات السريعة**

### إيه Redis؟
```
Redis = خزانة ذاكرة سريعة جداً
(مثل الرف على الجدار بدل الدرج في القبو)
```

### الوظائف:
```
✅ تخزين مؤقت (Cache)
✅ جلسات المستخدمين
✅ الرسائل المرتبطة (Pub/Sub)
✅ قوائم الانتظار (Queues)
✅ عدادات (Counters)
```

### البيانات:
```
مثال:
key: user_123
value: {name: "Ahmed", age: 25}

key: session_456
value: {token: "xyz789", expires: 3600}
```

### السرعة:
```
Redis: 1 ملي ثانية ⚡
PostgreSQL: 10 ملي ثانية 🐢
الذاكرة: 0.01 ملي ثانية ⚡⚡⚡
```

### متى تستخدمه:
```
✅ بيانات مؤقتة
✅ بيانات لا تحتاج دقة عالية
✅ عمليات سريعة جداً
❌ ليس للبيانات الهامة
```

---

## 🟦 **PostgreSQL - قاعدة البيانات الرئيسية**

### إيه PostgreSQL؟
```
PostgreSQL = قاعدة بيانات قوية جداً
(مثل المكتبة الكاملة بدل الرف البسيط)
```

### الوظائف:
```
✅ تخزين دائم (Persistent)
✅ جداول معقدة
✅ العلاقات بين الجداول
✅ التقارير والإحصائيات
✅ النسخ الاحتياطية
```

### الجداول:
```
جدول Users:
├─ id (المعرّف)
├─ username (الاسم)
├─ email (البريد)
└─ password (كلمة المرور)

جدول Attacks:
├─ id
├─ attacker_ip
├─ attack_type
├─ timestamp
└─ status
```

### السرعة:
```
PostgreSQL: 10 ملي ثانية
(أبطأ من Redis لكن أكثر أماناً)
```

### متى تستخدمه:
```
✅ بيانات هامة
✅ بيانات يجب حفظها
✅ عمليات معقدة
✅ تقارير وإحصائيات
```

---

## 📊 **Prometheus - جمع البيانات**

### إيه Prometheus؟
```
Prometheus = عامل البيانات
(يتجول في النظام ويجمع معلومات)
```

### الوظائف:
```
✅ جمع البيانات من الخدمات
✅ تخزين السلاسل الزمنية (Time Series)
✅ استعلامات قوية (PromQL)
✅ تنبيهات (Alerts)
✅ رصد الصحة (Health Check)
```

### البيانات:
```
metric_name{label="value"} = 42

مثال:
cpu_usage{host="server1"} = 45.5
memory_usage{container="redis"} = 512
http_requests_total{method="GET"} = 1000
```

### التخزين:
```
كل 15 ثانية يأخذ قراءة جديدة
ويخزنها في قاعدة TSDB الخاصة به
```

### متى تستخدمه:
```
✅ مراقبة الأداء
✅ رصد الأعطال
✅ إحصائيات النظام
✅ التنبيهات
```

---

## 🎨 **Grafana - عرض البيانات**

### إيه Grafana؟
```
Grafana = الفنان
(يأخذ البيانات الغبية ويرسمها بشكل جميل)
```

### الوظائف:
```
✅ رسوم بيانية جميلة
✅ لوحات تحكم (Dashboards)
✅ تنبيهات بصرية
✅ مشاركة البيانات
✅ تصدير التقارير
```

### الرسوم:
```
- رسم خط (Line Graph)
- عداد (Gauge)
- أعمدة (Bar Chart)
- فطيرة (Pie Chart)
- جدول (Table)
- حريطة (Heatmap)
```

### المثال:
```
بيانات Prometheus (غير مفهومة):
cpu_usage = 45.5
cpu_usage = 46.2
cpu_usage = 47.1
cpu_usage = 46.8

رسم Grafana (جميل):
 ███
██ █
█   ██
   CPU Usage ▁▂▃▄▅▆▇█
```

### متى تستخدمه:
```
✅ عرض الأداء
✅ مراقبة النظام
✅ تقارير تنفيذية
✅ عروض تقديمية
```

---

## 🔧 **Node Exporter - قراءة النظام**

### إيه Node Exporter؟
```
Node Exporter = جهاز قياس درجة الحرارة في البيت
(يقيس CPU, RAM, Disk, Network)
```

### الوظائف:
```
✅ قراءة استخدام CPU
✅ قراءة استخدام الذاكرة
✅ قراءة استخدام القرص
✅ قراءة الشبكة
✅ قراءة الملفات والعمليات
```

### البيانات:
```
node_cpu_seconds_total = 12345
node_memory_MemAvailable_bytes = 8589934592
node_filesystem_free_bytes = 107374182400
node_network_receive_bytes_total = 1234567890
```

### الاستخدام:
```
Prometheus يقرأ من Node Exporter
Node Exporter ← قراءات النظام
```

### متى تستخدمه:
```
✅ مراقبة النظام
✅ الموارد
✅ الأداء
```

---

## 🔄 **كيفية العمل معاً:**

```
┌──────────────────────────────────────────────┐
│                                              │
│  1. Node Exporter يقرأ النظام                │
│     (CPU = 45%, RAM = 512MB)                │
│                                              │
│  2. Prometheus يجمع البيانات                 │
│     (كل 15 ثانية)                          │
│                                              │
│  3. Grafana تأخذ البيانات من Prometheus     │
│                                              │
│  4. أنت تشوف الرسوم البيانية                 │
│     (الجميلة! 🎨)                           │
│                                              │
│  5. التنبيهات تُرسل إذا حصلت مشاكل          │
│                                              │
└──────────────────────────────────────────────┘
```

---

## 📱 **مثال عملي من Cyber Mirage:**

### السيناريو: اكتشاف هجوم

```
┌─────────────────────────────────────┐
│ 1. هجوم يحصل على النظام             │
│    (SQL Injection)                  │
└─────────────────────────────────────┘
            ↓
┌─────────────────────────────────────┐
│ 2. API يسجل الهجوم في PostgreSQL   │
│    (للحفظ الدائم)                   │
└─────────────────────────────────────┘
            ↓
┌─────────────────────────────────────┐
│ 3. API يخزن في Redis                │
│    (للوصول السريع)                  │
└─────────────────────────────────────┘
            ↓
┌─────────────────────────────────────┐
│ 4. Prometheus يأخذ البيانات          │
│    (attacks_detected_total = 245)   │
└─────────────────────────────────────┘
            ↓
┌─────────────────────────────────────┐
│ 5. Grafana تعرض الهجمات             │
│    (رسم بياني أحمر! 🔴)              │
└─────────────────────────────────────┘
            ↓
┌─────────────────────────────────────┐
│ 6. التنبيه يرن! 🚨                   │
│    (حذّر المسؤول)                   │
└─────────────────────────────────────┘
```

---

## 🎯 **المسؤوليات:**

| الخدمة | تجمع | تخزن | تعرض | تنبه |
|--------|------|------|------|------|
| **Redis** | ✅ | ✅ (مؤقت) | ❌ | ❌ |
| **PostgreSQL** | ✅ | ✅ (دائم) | ❌ | ❌ |
| **Prometheus** | ✅ | ✅ (TSDB) | ❌ | ✅ |
| **Grafana** | ❌ | ❌ | ✅ | ✅ |
| **Node Exporter** | ✅ | ❌ | ❌ | ❌ |

---

## 💾 **مثال على البيانات:**

### في Redis (مؤقت):
```
key: attack_session_123
value: {
  type: "SQL Injection",
  ip: "192.168.1.100",
  timestamp: 1698432000
}
# يُحذف بعد 1 ساعة
```

### في PostgreSQL (دائم):
```
INSERT INTO attacks VALUES (
  id: 245,
  type: "SQL Injection",
  source_ip: "192.168.1.100",
  timestamp: "2025-10-27 18:00:00",
  blocked: true
)
# محفوظ للأبد!
```

### في Prometheus (السلاسل الزمنية):
```
attacks_detected_total{type="SQL Injection"} = 245
attacks_detected_total{type="XSS"} = 120
attacks_detected_total{type="DoS"} = 85
# مع الوقت: 245 → 246 → 247 ...
```

### في Grafana (الرسم):
```
Attacks Over Time
┌──────────────────────┐
│ ▂▃▄▅▆▇█▇▆▅▄▃▂        │
│ 0  6h  12h  18h  24h │
└──────────────────────┘
SQL Injection (أحمر)
XSS (أصفر)
DoS (برتقالي)
```

---

## ✨ **الميزات الفريدة:**

### Redis:
- ⚡ سريع جداً جداً
- 💾 يحفظ الجلسات
- 🔄 Pub/Sub للرسائل

### PostgreSQL:
- 🔒 آمن جداً
- 📊 استعلامات معقدة
- 💪 قوي جداً

### Prometheus:
- 📈 تاريخ زمني دقيق
- 🚨 تنبيهات قوية
- 📋 لغة استعلام قوية

### Grafana:
- 🎨 جميل جداً
- 📊 لوحات قابلة للتخصيص
- 🔄 مشاركة سهلة

### Node Exporter:
- 📡 يقرأ النظام مباشرة
- ⚙️ خفيف الوزن
- 🔌 سهل التثبيت

---

## 🎓 **الخلاصة:**

```
تخزين سريع ← Redis
تخزين دائم ← PostgreSQL
جمع بيانات ← Prometheus
عرض جميل ← Grafana
قراءة النظام ← Node Exporter

معاً = نظام متكامل قوي! 💪
```

---

## 🚀 **كل الأوامر في مكان واحد:**

```powershell
# شغّل الكل
docker-compose up -d

# افتح المتصفح
http://localhost:9090  # Prometheus
http://localhost:3000  # Grafana
http://localhost:6379  # Redis (بدون واجهة)
http://localhost:5432  # PostgreSQL (بدون واجهة)

# توقف الكل
docker-compose down
```

---

**الآن أنت تفهم كل الخدمات وكيف تعمل معاً! 🎊**

استمتع! 🚀
