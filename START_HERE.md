# 🚀 START HERE - Quick Launch Guide

## ⚡ **أسهل طريقة للبداية (دقيقة واحدة)**

### **خطوة واحدة فقط:**

```powershell
.\start_defense.ps1
```

**ماذا سيحدث؟**
- ✅ سيفتح Dashboard تلقائياً
- ✅ سيفتح Honeypot Environment
- ✅ سيفتح Log Monitoring
- ✅ كل شيء جاهز للاستخدام!

### **افتح المتصفح:**
```
http://localhost:8501
```

---

## 🎯 **خيارات أخرى**

### **إذا كنت تريد:**

#### **1. تشغيل Dashboard فقط:**
```powershell
.\venv\Scripts\python.exe -m streamlit run src/dashboard/streamlit_app.py
```

#### **2. تشغيل Tests:**
```powershell
.\venv\Scripts\python.exe tests/run_all_tests.py
```

#### **3. تشغيل Benchmarks:**
```powershell
.\run_benchmarks.ps1
```

#### **4. تشغيل Docker (يحتاج Docker Desktop):**
```powershell
.\deploy_production.ps1 -Action start
```

---

## 📋 **البرامج المطلوبة**

### **✅ موجود بالفعل:**
- Python 3.13.5
- Virtual Environment
- كل المكتبات مثبتة

### **❌ اختياري (للـ Production):**
- Docker Desktop (فقط إذا أردت الطريقة 2)
- Kali Linux VM (فقط للاختبار)

---

## 🎓 **التشغيل السريع**

```powershell
# 1. افتح PowerShell
cd A:\cyber_mirage

# 2. شغّل
.\start_defense.ps1

# 3. افتح المتصفح
# http://localhost:8501

# ✅ Done!
```

---

## 📖 **للمزيد من التفاصيل:**
- اقرأ: **HOW_TO_START.md** (دليل كامل)
- اقرأ: **QUICKSTART.md** (بداية سريعة)
- اقرأ: **DOCKER_PRODUCTION_GUIDE.md** (Docker)

---

**🎯 لا تضيع وقتك - ابدأ الآن! 🎯**

```powershell
.\start_defense.ps1
```
